/**
 * Война как кампания (этап 84) — содержимое.
 *
 * Войну 0.6 вели дружины: отряд шёл к месту и разорял его. У войны не было ни
 * цели, ни фронтов, ни обоза — только броски и события. Кампания добавляет то,
 * без чего война не решение, а погода: зачем воюем, где именно, чем кормим и
 * чем всё кончилось.
 */

/** Чего добиваются войной (Ка1). */
export const CAMPAIGN_AIMS = ['takeLand', 'breakHost', 'forceTribute', 'defend'] as const
export type CampaignAim = (typeof CAMPAIGN_AIMS)[number]

export const AIM_DEFS: Record<
  CampaignAim,
  {
    readonly label: string
    readonly about: string
    /** Что считается победой. */
    readonly wins: string
  }
> = {
  takeLand: {
    label: 'взять землю',
    about: 'Дойти и сесть: чужие места переходят к тебе, и это видно на карте.',
    wins: 'взятые места',
  },
  breakHost: {
    label: 'разбить войско',
    about: 'Не земля, а люди: пока у них есть рать, любая земля вернётся.',
    wins: 'разбитые дружины',
  },
  forceTribute: {
    label: 'заставить платить',
    about: 'Не брать чужого, а сделать так, чтобы платили за спокойствие.',
    wins: 'дань, положенная миром',
  },
  defend: {
    label: 'отстоять своё',
    about: 'Ничего не взять и ничего не отдать: иногда это и есть победа.',
    wins: 'своё, оставшееся своим',
  },
}

/** Приказ своей части (Ка5). */
export const HOST_ORDERS = ['advance', 'siege', 'hold', 'forage', 'home'] as const
export type HostOrder = (typeof HOST_ORDERS)[number]

export const HOST_ORDER_DEFS: Record<
  HostOrder,
  { readonly label: string; readonly about: string }
> = {
  advance: {
    label: 'идти вперёд',
    about: 'Дойти до места и разорить его: быстро, громко и без стен.',
  },
  siege: { label: 'осадить', about: 'Встать под стенами и ждать: долго, дорого и надёжно.' },
  hold: { label: 'держать', about: 'Стоять там, где стоишь: прикрывать своё.' },
  forage: {
    label: 'кормиться с земли',
    about: 'Брать у тех, кто рядом. Войско сыто, округа помнит.',
  },
  home: { label: 'домой', about: 'Отвести людей к своей земле и дать им отдых.' },
}

/**
 * Снабжение (Ка3).
 *
 * Войско ест каждый день. Своя земля кормит из амбаров, чужая — только если с
 * неё берут; зимой не кормит ни та, ни другая.
 */
export const SUPPLY = {
  /** Сколько хлеба съедает человек в сутки. */
  perMan: 0.05,
  /** Сколько переходов до своей земли войско ещё считается снабжённым. */
  reach: 2,
  /** Сколько духа теряет голодное войско за сутки. */
  hungryMorale: 3,
  /** И какую долю людей теряет. */
  hungryLoss: 0.02,
  /** Насколько дороже обходится зима. */
  winter: 1.6,
  /** Что кормление с земли делает с памятью места за сутки. */
  forageMood: -1.2,
  /** И с разбоем вокруг. */
  forageBanditry: 0.03,
} as const

/** Донесение идёт не мгновенно (Ка5): столько суток на каждый переход. */
export const DISPATCH_DAYS = 1.5

export const CAMPAIGN_WORDS = {
  none: 'Кампании нет: воевать можно и так, но тогда это не война, а поход.',
  fed: 'Войско снабжено: обоз доходит.',
  forage: 'Войско кормится с земли: округа это помнит.',
  hungry: 'Войску нечего есть.',
} as const
