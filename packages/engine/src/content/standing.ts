/**
 * Дипломатия в числах (этап 195) — содержимое.
 *
 * К 1.0 дипломатии много: союзы, дань, договоры с тайными статьями, съезды,
 * посольства, резиденты. И всё это лежало по разным экранам: игрок видел
 * строки, но не расклад. Здесь лежит то, чем расклад читается.
 */

export const STANDING_BONDS = ['war', 'ally', 'tribute', 'paper', 'warm', 'cold'] as const
export type StandingBond = (typeof STANDING_BONDS)[number]

export const STANDING_BOND_DEFS: Record<
  StandingBond,
  { readonly label: string; readonly about: string; readonly weight: number }
> = {
  war: { label: 'война', about: 'Идёт война: всё прочее между вами не считается.', weight: -3 },
  ally: { label: 'союз', about: 'Входят в твои войны, и ты — в их.', weight: 3 },
  tribute: { label: 'дань', about: 'Платят или платишь: это помнят обе стороны.', weight: 1 },
  paper: { label: 'бумага', about: 'Договор жив, пока не вышел срок.', weight: 2 },
  warm: { label: 'приязнь', about: 'Бумаги нет, но говорят охотно.', weight: 1 },
  cold: { label: 'холод', about: 'Бумаги нет, и говорить не о чем.', weight: -1 },
}

export const STANDING = {
  /** Отношение, выше которого это приязнь. */
  warmAt: 30,
  /** И ниже которого — холод. */
  coldAt: -30,
  /** За сколько суток до конца срока о нём пора думать. */
  soonDays: 365,
  /** Сколько ходов показывать: сводка кончается делом, а не списком. */
  shows: 4,
} as const

export const STANDING_WORDS = {
  one: 'Весь расклад — на одном месте: кто с кем, против кого и с какого дня.',
  why: 'У всякой связи есть день, с которого она есть, и причина, по которой она держится.',
  ahead: 'Если не менять ничего:',
  moves: 'Сводка кончается ходами, а не таблицей.',
  theirs: 'Тот же расклад с чужого места — но видно в нём столько, сколько видно тебе.',
  hidden: 'тайная статья есть, и не тебе о ней знать',
} as const
