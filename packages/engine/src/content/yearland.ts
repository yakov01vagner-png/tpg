/**
 * Год, погода и земля (этап 180) — содержимое.
 *
 * Времена года с 0.5 меняют урожай и скорость дороги, погода дня — видимость и
 * ход. Всё это — проценты, и потому год в игре был штрафом, а не временем: он
 * ничего не разрешал и ничего не запрещал. Здесь лежат правила, которые год
 * меняет, и слова, которыми год называется.
 */

export const YEARLAND = {
  /** Ниже какого урожая год считается недородным. */
  leanAt: 0.85,
  /** И выше какого — щедрым. */
  fatAt: 1.1,
  /** С какой усталости земли поле начинает родить заметно хуже. */
  tiredAt: 0.4,
  /** Сколько суток мороза нужно, чтобы реки встали. */
  freezeFrom: 320,
  freezeTo: 40,
  /** Насколько засуха поднимает угрозу пожара. */
  droughtFire: 0.5,
  /** И мокрый год — падежа. */
  wetMurrain: 0.4,
  /** С какой доли скота падёж считается бедствием. */
  murrainAt: 0.25,
  /** С какой доли дворов — пожар. */
  fireAt: 0.15,
} as const

export const YEAR_KINDS = ['lean', 'even', 'fat'] as const
export type YearKind = (typeof YEAR_KINDS)[number]

export const YEAR_DEFS: Record<YearKind, { readonly label: string; readonly says: string }> = {
  lean: {
    label: 'недород',
    says: 'Год был мокрый, и хлеб не убрали: что стояло в поле, то и сгнило.',
  },
  even: { label: 'ровный год', says: 'Год вышел ровный: ни голода, ни лишнего.' },
  fat: { label: 'щедрый год', says: 'Год выдался щедрый: амбары полны, и хлеб дёшев.' },
}

export const MISHAPS = ['fire', 'murrain', 'flood', 'frost'] as const
export type MishapId = (typeof MISHAPS)[number]

export const MISHAP_DEFS: Record<
  MishapId,
  { readonly label: string; readonly from: string; readonly costs: string }
> = {
  fire: {
    label: 'пожар',
    from: 'сухое лето и тесная застройка',
    costs: 'Выгорела улица: людям негде зимовать, и часть уйдёт.',
  },
  murrain: {
    label: 'падёж',
    from: 'мокрый год и скученный скот',
    costs: 'Пал скот: пахать не на чем, и мяса не будет до осени.',
  },
  flood: {
    label: 'половодье',
    from: 'весна и река под боком',
    costs: 'Вода стоит в полях: сев отложен, броды закрыты.',
  },
  frost: {
    label: 'заморозок',
    from: 'поздняя весна',
    costs: 'Мороз побил всходы: пересевать нечем и некогда.',
  },
}

export const YEARLAND_WORDS = {
  frozen: 'Реки встали: где летом брод, там зимой дорога, — и войско идёт напрямик.',
  flood: 'Половодье: броды под водой, обозы стоят.',
  tired: 'Земля устала: поле родит хуже, сколько его ни паши.',
  rested: 'Земля отдохнула и родит как прежде.',
  rule: 'Погода не отнимает долю — она меняет правило.',
} as const
