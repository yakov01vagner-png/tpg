/**
 * Летопись, которая судит (этап 147) — содержимое.
 *
 * Летопись 0.6 была журналом: строки по годам, и ничего кроме. Здесь она
 * становится счётом — тем, что мир помнит о тебе, — и счёт этот входит в
 * признание, страх и цену.
 */

/** Род дела: чем оно запомнится и насколько долго. */
export const MARKS = ['glory', 'blood', 'word', 'care', 'shame'] as const
export type MarkId = (typeof MARKS)[number]

export const MARK_DEFS: Record<
  MarkId,
  {
    readonly label: string
    readonly about: string
    /** Вес одного такого дела. */
    readonly weight: number
    /** Сколько лет помнится; 0 — не забывается никогда. */
    readonly years: number
    /** Добром или худом это помнится. */
    readonly good: boolean
  }
> = {
  glory: {
    label: 'слава',
    about: 'Победы, венчания, соборы: то, что поминают на пирах.',
    weight: 3,
    years: 60,
    good: true,
  },
  blood: {
    label: 'кровь',
    about: 'Взятые города и сожжённые сёла: помнят дольше, чем славу.',
    weight: 4,
    years: 90,
    good: false,
  },
  word: {
    label: 'слово',
    about: 'Нарушенные грамоты и поручительства: это не забывается вовсе.',
    weight: 6,
    years: 0,
    good: false,
  },
  care: {
    label: 'забота',
    about: 'Хлеб в недород, суд по правде, выкупленные пленные.',
    weight: 2,
    years: 40,
    good: true,
  },
  shame: {
    label: 'позор',
    about: 'Бегство, голод в своей земле, покаяние при всех.',
    weight: 3,
    years: 50,
    good: false,
  },
}

/** По каким словам дело узнаётся в летописи: летопись пишется словами. */
export const MARK_WORDS: Record<MarkId, readonly string[]> = {
  glory: ['венчан', 'победа', 'разбил', 'собор', 'признаёт', 'признали'],
  blood: ['взят', 'сожж', 'разорил', 'приступ', 'резня'],
  word: ['порван', 'нарушил', 'не пришёл', 'отозвал'],
  care: ['хлеб', 'выкупил', 'суд', 'отстроил', 'кормил'],
  shame: ['бежал', 'покаял', 'голод', 'позор', 'интердикт'],
}

export const ANNALS = {
  /** Во что обходится своя летопись — за каждый год, который в ней пишут. */
  perYear: 400,
  /** Сколько своей летописи верят сверх дел: доля от настоящего счёта. */
  addsAtMost: 0.35,
  /** Насколько чужой взгляд искажает счёт: доля. */
  slant: 0.4,
  /** Насколько память входит в страх (этап 135). */
  intoDread: 0.25,
  /** И насколько она сбивает цену непризнанности (этап 138). */
  intoPrice: 0.15,
} as const

export const ANNALS_WORDS = {
  counted: 'Летопись — не журнал, а счёт: мир помнит дела, и память эта имеет цену.',
  forever: 'Нарушенное слово не забывается вовсе: прочее меркнет, это остаётся.',
  slanted: 'У чужой летописи свой взгляд: враг помнит твою кровь, друг — твою славу.',
  own: 'Своя летопись стоит серебра и правды: приписать можно, но немного.',
} as const
