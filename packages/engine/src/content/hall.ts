/**
 * Двор, который живёт (этап 168) — содержимое.
 *
 * С этапа 104 у должности есть человек: нрав, годы, цель и голос. Чего не было
 * — двора как целого. Люди стояли в столбик и не знали друг о друге: не с кем
 * было сойтись против третьего, некого было потерять и не из-за чего. Здесь
 * лежит то, чем двор держится: связи, услуги и цена ухода.
 */

export const TIE_KINDS = ['friend', 'foe', 'kin', 'party'] as const
export type TieKind = (typeof TIE_KINDS)[number]

export const TIE_DEFS: Record<
  TieKind,
  { readonly label: string; readonly about: string; readonly weight: number }
> = {
  friend: {
    label: 'дружба',
    about: 'Держатся вместе и просят за друг друга.',
    weight: 2,
  },
  foe: {
    label: 'вражда',
    about: 'Один не поднимется, пока стоит другой: всякая милость тебе одному — обида другому.',
    weight: -3,
  },
  kin: {
    label: 'родство',
    about: 'Свояки: падёт один — потянет за собой второго.',
    weight: 3,
  },
  party: {
    label: 'общая выгода',
    about: 'Хотят одного и того же и потому держатся заодно, пока хватает на обоих.',
    weight: 1,
  },
}

export const HALL = {
  /** Раз во сколько суток двор считает своё. */
  beat: 30,
  /** Сколько верности тянет вражда при дворе за такт. */
  feudCosts: -2,
  /** И прибавляет дружба. */
  friendGives: 1,
  /** Ниже какой верности он начинает смотреть на сторону. */
  slippery: 30,
  /** И ниже какой — уходит или изменяет. */
  breaks: 18,
  /** Насколько выгоднее изменить тому, у кого при дворе враг. */
  feudTempts: 8,
  /** Сколько лет службы считается выслугой. */
  risenYears: 8,
} as const

export const HALL_WORDS = {
  feud: 'При дворе вражда, и она стоит верности обоим.',
  quiet: 'Двор спокоен: ни свояков, ни врагов — одни служащие.',
  slipping: 'Он начал смотреть на сторону.',
  betrayed: 'Он ушёл не пустым: унёс то, что знал.',
  risen: 'Выслужился: столько лет при тебе — уже не служба, а род.',
  costs: 'Место опустело, и держава это чувствует.',
} as const
