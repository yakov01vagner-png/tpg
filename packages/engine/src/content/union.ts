/**
 * Путь короны (этап 131) — содержимое.
 *
 * Объединить мир — не «взять сорок мест». Взять можно и сорок, а признавать тебя
 * никто не станет. Здесь у объединения появляются четыре двери, через которые
 * берётся чужое признание, и три условия, без которых оно ничего не значит:
 * последние держатся крепче первых, взятое надо удержать, и мир после
 * объединения играется иначе.
 */

/** Четыре двери к чужому признанию. */
export const DOORS = ['war', 'treaty', 'kin', 'coin'] as const
export type DoorId = (typeof DOORS)[number]

export const DOOR_DEFS: Record<
  DoorId,
  {
    readonly label: string
    readonly about: string
    /** Чем берут. */
    readonly costs: string
    /** Что остаётся после: признание такой ценой помнят по-разному. */
    readonly after: string
  }
> = {
  war: {
    label: 'войной',
    about: 'Разбить и заставить признать. Быстрее всего и дороже всего.',
    costs: 'людьми и годами войны',
    after: 'Признание под мечом помнят как обиду: оно первым и отваливается.',
  },
  treaty: {
    label: 'договором',
    about: 'Уступить то, что ему нужно, и получить подпись.',
    costs: 'землёй, пошлиной или тайной статьёй',
    after: 'Купленное согласием держится, пока держится договор.',
  },
  kin: {
    label: 'родством',
    about: 'Войти в его дом: родня не признаёт — родня уже родня.',
    costs: 'приданым и свободой в браках',
    after: 'Родство держится дольше всего и переходит к наследникам.',
  },
  coin: {
    label: 'серебром',
    about: 'Дар, заём или откуп: признать выгоднее, чем не признавать.',
    costs: 'казной, и платить придётся не однажды',
    after: 'Купленное серебром стоит, пока платишь.',
  },
}

export const UNION = {
  /** Насколько дороже каждое следующее признание, когда осталось мало. */
  lastHarder: 1.4,
  /** Со скольких непризнавших начинается «последние». */
  lastFrom: 3,
  /** Сколько лет объединение должно продержаться, чтобы считаться концом. */
  holdYears: 10,
  /** Сколько стоит дар за признание — на место чужой земли. */
  giftPerPlace: 180,
  /** Насколько дешевле признание тому, кто уже венчан. */
  crownedEasier: 0.85,
  /** Насколько недовольнее держава, где все под одной рукой. */
  unionUnrest: 14,
  /** Сколько своих мятежей приходит на место чужих войн. */
  unionRevolts: 1.6,
} as const

export const UNION_WORDS = {
  notPlaces:
    'Объединение — не число мест: пока есть корона, которая тебя не признала, ты первый среди спорящих, а не государь.',
  four: 'К чужому признанию четыре двери, и у каждой своя цена.',
  last: 'Последние держатся крепче первых: их берут не силой.',
  hold: 'Взятое надо удержать: путь проверяется не в день победы, а через десять лет.',
  lost: 'Признание отвалилось: круг пошёл заново.',
  world: 'Мир под одной рукой играется иначе: чужих войн нет, своих недовольных больше.',
} as const
