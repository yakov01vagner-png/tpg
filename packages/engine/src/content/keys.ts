/**
 * Порядок ключей, который не должен решать (этап 208) — содержимое.
 *
 * В состоянии полно записей-словарей: поселения, отношения корон, метки, дела.
 * Порядок ключей в них — не данные: он такой, каким его оставила запись, и
 * после сейва, миграции или другого пути может выйти иным. Всё, что решает по
 * этому порядку, решает по случайности, которой нет в правилах.
 *
 * Здесь лежит то немногое, что выбрано рукой, и перечень словарей, по которым
 * мир ходит чаще всего.
 */

export const KEY_ORDER = {
  /** Сколько разных перемешиваний проверять: одного мало. */
  shuffles: 5,
  /** Сколько команд играть после перемешивания. */
  chain: 40,
} as const

/**
 * Словари состояния, порядок ключей в которых ни на что не должен влиять.
 *
 * Перечень нужен не коду, а проверке: по нему видно, что перемешивали именно
 * то, по чему мир и ходит.
 */
export const KEY_SHUFFLED = [
  'settlements',
  'politics.relations',
  'politics.archmages',
  'marks',
  'dealings',
  'deeds',
  'lordDeeds',
  'fame',
  'reputation',
  'knowledge',
] as const

/**
 * Скелет мира — исключение, и это надо назвать.
 *
 * `world` рождается из зерна и никогда не пересобирается: порядок его ключей —
 * такие же данные, как порядок в массиве, и по нему мир ходить вправе. Взамен
 * с него спрашивается другое: порядок обязан переживать запись, загрузку и
 * миграцию, и обязан выходить одним и тем же из одного зерна.
 */
export const KEY_SKELETON = 'world' as const

export const KEY_WORDS = {
  same: 'Порядок ключей — не данные: мир от него не меняется.',
  drift: 'Мир разошёлся от перемешивания ключей — значит, где-то решают по порядку.',
  canon: 'Сверка идёт по приведённому виду: ключи отсортированы, остаётся только суть.',
} as const
