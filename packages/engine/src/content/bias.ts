/**
 * Он ошибается и упорствует (этап 120) — содержимое.
 *
 * Картина мира (118) и выводы (119) сделали ИИ похожим на игрока в том, что он
 * знает не всё. Здесь он становится похож в том, чего игроку не хватало в 0.7:
 * он видит то, что ждёт увидеть, держится своего заблуждения и бросает его
 * только когда его опровергнут слишком громко.
 */

/** Чем именно он предубеждён. */
export const BIASES = ['proud', 'fearful', 'greedy', 'cold'] as const
export type BiasKind = (typeof BIASES)[number]

export const BIAS_DEFS: Record<
  BiasKind,
  {
    readonly label: string
    readonly about: string
    /** Как он смотрит на чужое: доля, на которую расходится с правдой. */
    readonly onOthers: number
    /** И на своё. */
    readonly onSelf: number
    /** Насколько громким должно быть опровержение, чтобы он передумал. */
    readonly stubborn: number
  }
> = {
  proud: {
    label: 'надменный',
    about: 'Своих считает лучшими, чужих — сбродом. Переоценивает себя и недооценивает всех.',
    onOthers: -0.25,
    onSelf: 0.3,
    stubborn: 0.45,
  },
  fearful: {
    label: 'пугливый',
    about: 'Видит угрозу там, где её нет. Чужое войско ему всегда больше своего.',
    onOthers: 0.3,
    onSelf: -0.15,
    stubborn: 0.3,
  },
  greedy: {
    label: 'жадный',
    about: 'Считает не копья, а доход. Слабых видит богатыми, богатых — слабыми.',
    onOthers: -0.12,
    onSelf: 0.1,
    stubborn: 0.35,
  },
  cold: {
    label: 'расчётливый',
    about: 'Ошибается меньше всех и меняет мнение быстрее всех. Зато и не рискует.',
    onOthers: 0.05,
    onSelf: 0.05,
    stubborn: 0.2,
  },
}

export const BIAS = {
  /** Раз во сколько суток он пересматривает то, во что верит. */
  beat: 20,
  /** Насколько должно расходиться новое с прежним, чтобы это было «громко». */
  loud: 0.35,
  /** Насколько поражение громче любой вести. */
  battleLoud: 1.2,
  /** Сколько суток заблуждение держится само по себе, если его не тревожить. */
  holds: 540,
} as const

export const BIAS_WORDS = {
  expects: 'Он видит то, что ждёт увидеть.',
  selfHigh: 'Своё войско он считает лучше, чем оно есть.',
  selfLow: 'Своё войско он считает хуже, чем оно есть.',
  holds: 'Заблуждение держится: тихих вестей ему не довольно.',
  woke: 'Он понял, что ошибался, — и переменил партию.',
  cost: 'Война, начатая по ошибке.',
  says: 'Ошибку видно словами, а не числом на экране.',
} as const
