/**
 * Что есть у чужой стороны (этап 162) — содержимое.
 *
 * До 1.0 половина мерок пути считалась только для игрока: у короны не было ни
 * благочестия, ни дел, ни родни, ни ранга — там стояли нули и заглушки. Из-за
 * этого три пути из пяти были для корон закрыты, а из открытых они шли по
 * половине вех. Ни одной новой величины в состояние это не добавляет: всё
 * выводится из скелета мира (обители, порты, рудники, города), из нрава и из
 * колена. Здесь лежат веса, по которым выводится.
 */

export const THEIRS = {
  /** Благочестие за обитель в своей земле. */
  perMonastery: 14,
  /** И за уклад, в котором церковь — половина власти. */
  holyFlavour: 34,
  /** Сколько благочестия съедает каждая война, которую она ведёт сейчас. */
  warSin: 7,
  /** Ниже этого церковь к ней не спокойна. */
  calmAt: 26,
  /** С этого — воюет с её слова, а не со своего. */
  crusadeAt: 48,
  /** Дело: порт, рудник и город кормят корону сами. */
  perPort: 1,
  perMine: 1,
  perCity: 1,
  /** И уклад, который стоит на торге, — ещё два. */
  tradeFlavour: 2,
  /** Серебро в казне: за место, за город, за порт, за рудник. */
  coinPerPlace: 380,
  coinPerCity: 2600,
  coinPerPort: 3400,
  coinPerMine: 3000,
  /** Дань считается тем, что она принесёт за год. */
  tributeDays: 365,
  /** Родство: из скольких сотен пар выходит брак между коронами. */
  kinChance: 26,
  /** Ранг придворного чародея: до какой ступени он вообще доходит. */
  mageTiers: 5,
  /** Насколько уклад, знающий чары, поднимает ступень. */
  mageFlavour: 2,
  /** С какой ступени у двора заводятся вещи с чарами. */
  artifactsFrom: 3,
  /** Сколько умения стоит одна ступень. */
  skillPerTier: 16,
} as const

/** Уклады, в которых церковь весит больше обычного. */
export const HOLY_WORDS = ['свят', 'церков', 'вер', 'храм', 'обител'] as const

/** Уклады, которые стоят на торге. */
export const TRADE_WORDS = ['торг', 'гильд', 'хартия', 'купеч', 'вольных городов'] as const

/** Уклады, где чары — часть власти, а не диковина. */
export const MAGE_WORDS = ['чар', 'маг', 'жил', 'штольн', 'колод', 'лес'] as const

export const THEIRS_WORDS = {
  mage: 'При дворе сидит чародей, и ступень у него не ниже твоей.',
  poor: 'Казна её считается по земле: чужого серебра никто не пересчитывал.',
} as const
