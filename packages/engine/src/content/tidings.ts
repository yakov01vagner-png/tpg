/**
 * Короны видят друг друга (этап 164) — содержимое.
 *
 * Слой знания 0.8 умел всё, что нужно, но работал он в одну сторону: вести
 * заводил игрок, а между коронами мир оставался прозрачным — они считали чужую
 * силу догадкой из расстояния и нрава, и догадка эта не старела, не ходила по
 * карте и не бывала чужой ошибкой. Здесь лежат веса вестей, которые короны
 * посылают друг о друге без всякого игрока.
 */

export const TIDINGS = {
  /** Раз во сколько суток по миру расходятся вести. */
  beat: 20,
  /** Сколько вестей за такт переносит молва от одной короны к другой. */
  hops: 4,
  /** На каком расстоянии по карте земли считаются соседними. */
  borderSpan: 420,
  /** Насколько врёт то, что доносят свои: доля от правды. */
  ownOff: 0.06,
  /** И то, что привёз посол. */
  envoyOff: 0.15,
  /** И то, что говорят на торгу. */
  rumourOff: 0.35,
  /** Старее этого весть решений уже не определяет. */
  stale: 400,
  /** Насколько должно разойтись знание с правдой, чтобы счесть решение неверным. */
  wrongAt: 0.25,
  /** Во сколько раз пущенный игроком слух занижает того, о ком он пущен. */
  taleOff: 0.65,
} as const

export const TIDINGS_WORDS = {
  near: 'Соседу доносят свои: такая весть врёт мало и стареет медленно.',
  far: 'До дальнего доходит торгом: половина правды и вся молва.',
  travels: 'Ошибка ходит по карте вместе с вестью: соврали одному — поверят трое.',
  tale: 'Твой слух лёг не в разговоры, а в их счёт: они теперь считают его слабее.',
} as const
