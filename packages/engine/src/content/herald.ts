/**
 * Вести с войны (этап 175) — содержимое.
 *
 * О бое в журнале появлялась строка, и появлялась она мгновенно и точно.
 * Оттого война, которую ведёшь не ты, была таблицей: не бывало ни запоздавшей
 * вести, ни привранной, ни двух победителей одной битвы. Здесь лежит то, из
 * чего складывается реляция.
 */

export const HERALD = {
  /** Сколько суток идёт весть за один переход. */
  perHop: 1.5,
  /** Насколько весть привирает в свою пользу. */
  ourGild: 0.25,
  /** И насколько мельчит чужой успех. */
  theirHide: 0.2,
  /** Сколько суток весть считается свежей. */
  fresh: 60,
  /** Насколько проигранная война роняет настроение своих мест. */
  defeatUnrest: -8,
  /** И насколько выигранная его поднимает. */
  victoryCheer: 5,
  /** Сколько реляций помнится. */
  remembers: 40,
  /** Насколько расходится с правдой весть, чтобы счесть решение по ней неверным. */
  wrongAt: 0.2,
} as const

export const HERALD_WORDS = {
  came: 'Пришла реляция с войны.',
  late: 'Весть шла долго: пока она шла, там всё переменилось.',
  ours: 'Наши пишут о победе.',
  theirs: 'У них об этом бое пишут своё — и тоже о победе.',
  both: 'Обе стороны празднуют одну и ту же битву. В летописи останутся обе.',
  home: 'Проигранная война доходит до своих: в местах ропщут.',
  wrong: 'Решение принято по вести, которая врала.',
} as const
