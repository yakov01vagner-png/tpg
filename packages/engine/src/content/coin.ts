/**
 * Казна короны как вещь (этап 165) — содержимое.
 *
 * До 1.0 чужого серебра в игре не было. Казна короны считалась по числу мест
 * («мест ×420»), и потому её нельзя было ни истощить, ни наполнить: война не
 * стоила ей ничего, дань ничего не приносила, а заём игрока (этап 133) уходил
 * в пустоту. Здесь у неё появляется число, которое растёт и тратится, и веса
 * этого числа лежат тут.
 */

export const COIN = {
  /** Раз во сколько суток сводится казна короны. */
  beat: 30,
  /** Подать: сколько серебра в сутки даёт тысяча жителей. */
  taxPerThousand: 5.6,
  /** Пошлина с города и с порта в сутки. */
  cityToll: 18,
  portToll: 28,
  /** И доход с рудника. */
  mineYield: 22,
  /** Жалованье: сколько стоит корона один человек в поле в сутки. */
  manWage: 0.2,
  /** И один человек за стенами. */
  guardWage: 0.2,
  /** Двор: сколько стоит корона одно её место в сутки. */
  courtPerPlace: 3,
  /** Во сколько раз дороже обходится войско, пока корона воюет: мирный год она сводит с прибытком, военный — в убыток. */
  warCosts: 1.8,
  /** Ниже этого казна считается пустой. */
  emptyAt: 2000,
  /** Сколько серебра корона берёт в долг, когда ей нечем платить. */
  borrows: 12000,
  /** Насколько охотнее мирится та, у которой пусто. */
  pooreHaste: 2.2,
  /** И насколько неохотнее объявляет войну. */
  poorPressure: 0.45,
  /** Сколько платит корона по долгу за такт, долей от долга. */
  paysDebt: 0.05,
} as const

export const COIN_WORDS = {
  empty: 'Казна пуста: войско расходится, двор ропщет, а мира ищут первыми.',
  rich: 'Серебра у неё столько, что войну она наймёт, а не поведёт.',
  borrowed: 'Она заняла — у кого пришлось и под какой процент дали.',
  unseen: 'Чужая казна известна не точно: её считают по земле и по слухам.',
} as const
