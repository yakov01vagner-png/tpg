/**
 * Потери, раненые и ветераны (этап 174) — содержимое.
 *
 * Раненые в большом бою возвращались в строй в тот же день: доля павших просто
 * прибавлялась обратно. Оттого война не оставляла следа — ни лазарета, ни
 * выплат семьям, ни разницы между отрядом, вышедшим из войны, и набранным
 * заново. Здесь лежит то, чем потери считаются.
 */

export const SCARS = {
  /** Какая доля павших на самом деле ранена, если поле осталось за тобой. */
  woundedShare: 0.35,
  /** И какая — если поле осталось за чужими: этих добьют. */
  lostFieldShare: 0.08,
  /** Сколько суток встаёт на ноги раненый без лекаря. */
  healDays: 45,
  /** Во сколько раз быстрее с лекарем. */
  healer: 0.5,
  /** И в обители. */
  monastery: 0.65,
  /** И дома. */
  home: 0.75,
  /** Какая доля раненых не встанет вовсе. */
  diesOfWounds: 0.18,
  /** Сколько платят семье за павшего. */
  bloodMoney: 120,
  /** Насколько падает верность мест, если за павших не плачено. */
  unpaidAnger: -3,
  /** Раз во сколько суток считается лазарет. */
  beat: 10,
  /** Сколько войн делает человека ветераном. */
  veteranWars: 3,
  /** Сколько раненых помнится поимённо. */
  remembers: 60,
} as const

export const SCARS_WORDS = {
  wounded: 'Раненых снесли в обоз. Кто встанет — встанет не завтра.',
  died: 'Не все раны заживают: часть раненых не встала.',
  healed: 'Из лазарета вернулись в строй.',
  paid: 'За павших заплачено семьям: земля это помнит.',
  unpaid: 'За павших не плачено, и вдовы считают дни.',
  veteran: 'Прошедший три войны стоит двух новобранцев — и знает это.',
  after: 'Через год после большой войны от дружины остаётся не то, что вышло в поход.',
} as const
