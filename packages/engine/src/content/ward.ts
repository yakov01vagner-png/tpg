/**
 * Гарантия и покровительство (этап 137) — содержимое.
 *
 * В игре было два способа связать себя с чужой короной: договор (0.7) и дань
 * (0.6). Историческое равновесие держалось на третьем — на поручительстве: за
 * слабого ручается сильный, и тронувший слабого получает войну с сильным. Это
 * не союз: слабый ничего не обещает взамен, кроме того, что он слабый.
 */

/** Чем один связывает себя с другим, не беря его себе (Га1, Га2). */
export const BONDS = ['guarantee', 'hand'] as const
export type BondId = (typeof BONDS)[number]

export const BOND_DEFS: Record<
  BondId,
  { readonly label: string; readonly about: string; readonly after: string }
> = {
  guarantee: {
    label: 'гарантия',
    about: 'Поручиться за чужую землю: тронут его — ты в войне, и об этом знают все.',
    after: 'Поручившийся связан: на того, за кого поручился, не ходят.',
  },
  hand: {
    label: 'под рукой',
    about: 'Слабая корона идёт под руку сильной: не дань и не вассалитет, а третье.',
    after: 'Под чьей-то рукой корона признаёт не за себя: её признание считается за руку.',
  },
}

/** Чего нельзя поручившемуся (Га3). */
export const TIED = ['war', 'ally', 'tribute'] as const
export type TiedId = (typeof TIED)[number]

export const TIED_DEFS: Record<TiedId, { readonly label: string; readonly says: string }> = {
  war: {
    label: 'война с ним',
    says: 'На того, за кого поручился, не ходят: это и есть цена поручительства.',
  },
  ally: {
    label: 'союз с его врагом',
    says: 'Союз с тем, кто на него идёт, — то же самое, что нарушить слово.',
  },
  tribute: {
    label: 'дань с него',
    says: 'С того, за кого ручаешься, дани не берут: взял — значит, не поручился, а подчинил.',
  },
}

export const WARD = {
  /** Во сколько раз поручитель должен быть сильнее того, за кого ручается. */
  strongerBy: 1.4,
  /** Сколько суток есть на то, чтобы прийти, когда позвали. */
  comeDays: 90,
  /** Во что обходится нарушенное поручительство — против всего мира. */
  breaksWord: -30,
  /** И насколько это дороже нарушенного договора (0.7). */
  worseThanTreaty: 2,
  /** Раз во сколько суток короны берут слабых под руку и ручаются друг за друга. */
  beat: 30,
  /**
   * Насколько сильнее должна быть рука, чтобы под неё пошли.
   *
   * Мерка та же, что у поручительства: и то и другое — про того, кто заведомо
   * сильнее. Двух разных чисел тут не нужно.
   */
  /** Сколько весит дальняя рука против соседней: 0 — вовсе ничего. */
  farPatron: 0.35,
} as const

export const WARD_WORDS = {
  given: 'Ты поручился: теперь его война — твоя война, и мир это записал.',
  bound: 'Поручительство связывает руки: пока оно есть, некоторых ходов нет.',
  called: 'За того, за кого ты поручился, взялись. Мир смотрит, придёшь ли.',
  broken: 'Ты не пришёл, когда поручился. Это помнят дольше порванной грамоты.',
  kept: 'Ты пришёл, когда позвали. Слово, подтверждённое войной, стоит дороже бумаги.',
  hand: 'Он идёт под твою руку: не данник и не вассал, а тот, за кого ты отвечаешь.',
  theirs: 'Короны делают то же самое: берут слабых под руку и ручаются друг за друга.',
} as const
