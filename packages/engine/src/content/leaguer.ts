/**
 * Осада, которая читается (этап 172) — содержимое.
 *
 * Осада считалась по частям: запас отдельно, стены отдельно, подкоп отдельно,
 * вылазки отдельно, а шанс сдачи — своей формулой поверх всего. Смотреть на
 * это было нельзя: под стенами шли сутки, а не осада. Здесь лежит то, из чего
 * складывается один счёт на обе стороны.
 */

export const LEAGUER = {
  /** С какого дня осады в лагере и в городе начинается мор. */
  sicknessFrom: 20,
  /** Насколько мор растёт за каждые десять суток сверх. */
  sicknessPer10: 0.06,
  /** Во сколько раз теснота города поднимает мор. */
  crowding: 1.4,
  /** Сколько суток стоит одна машина у стен в счёте срока. */
  enginesWorth: 6,
  /** Сколько суток снимает пролом. */
  breachWorth: 12,
  /** Ниже какой сытости горожане начинают говорить о воротах. */
  townsfolkAt: 0.55,
  /** И гарнизон. */
  garrisonAt: 0.3,
  /** И сам владетель. */
  lordAt: 0.12,
  /** Сколько суток держится город на полном запасе сверх еды: воля, а не хлеб. */
  willDays: 30,
  /** За сколько переходов подмога считается близкой. */
  reliefHops: 6,
  /** Во что обходится свободный выход: доля добычи, которую он отнимает. */
  freeExitCosts: 0.5,
  /** Раз во сколько суток осада рассказывается. */
  weekDays: 7,
} as const

export const INSIDE = ['townsfolk', 'garrison', 'lord'] as const
export type InsideId = (typeof INSIDE)[number]

export const INSIDE_DEFS: Record<
  InsideId,
  { readonly label: string; readonly holds: string; readonly breaks: string }
> = {
  townsfolk: {
    label: 'горожане',
    holds: 'Пока в лавках есть чем торговать, город терпит.',
    breaks: 'Горожане говорят о воротах: им всё равно, чей герб над башней, если дети едят.',
  },
  garrison: {
    label: 'гарнизон',
    holds: 'Гарнизон стоит: им платят и им есть чем гордиться.',
    breaks: 'Гарнизон считает дни: солдат голоден и знает, что подмоги нет.',
  },
  lord: {
    label: 'владетель',
    holds: 'Владетель не слышит о сдаче: ему терять больше всех.',
    breaks: 'Даже владетель заговорил об условиях: дальше держать нечем.',
  },
}

export const LEAGUER_WORDS = {
  quiet: 'Неделя прошла тихо: под стенами копают, в городе считают хлеб.',
  hungry: 'В городе начали резать скот.',
  sick: 'В лагере и в городе кашляют: осада кормит болезнь лучше, чем людей.',
  sally: 'Была вылазка: ночью резали часовых у машин.',
  breach: 'Кладка села: в стене пролом, и обе стороны это видят.',
  relief: 'Говорят, идёт подмога.',
  exit: 'Просят свободный выход: уйти с оружием и без обоза.',
} as const
