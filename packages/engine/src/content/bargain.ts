/**
 * Торг, который живёт (этап 178) — содержимое.
 *
 * Цена считалась одним: насколько товара меньше, чем нужно. Оттого рынок был
 * таблицей — она честная, но в ней не видно ни войны, ни дороги, ни того, что
 * год выдался мокрый. Здесь лежит то, из чего цена складывается сверх нужды.
 */

export const BARGAIN = {
  /** Во сколько раз дороже товар там, куда идёт война. */
  war: 1.35,
  /** И в разорённой округе. */
  raided: 1.25,
  /** Насколько дешевле там, где этот товар делают (порт — рыба, рудник — железо). */
  atSource: 0.8,
  /** Насколько дорожает всё, что везут издалека: за каждый переход. */
  perHop: 0.04,
  /** Сколько переходов считается далёким. */
  farHops: 6,
  /** Насколько недород поднимает цену хлеба. */
  badYear: 1.4,
  /** И богатый год роняет. */
  goodYear: 0.8,
  /** Пошлина вольного города: доля от цены. */
  toll: 0.1,
  /** Насколько дорога с разбоем поднимает цену перевозки. */
  banditry: 0.5,
} as const

export const BARGAIN_WORDS = {
  war: 'Здесь ждут войну, и всё, что можно увезти, дорожает.',
  raided: 'В округе жгли: привозного мало, своего меньше.',
  source: 'Здесь это делают — здесь это и дёшево.',
  far: 'Сюда это везут издалека, и дорога записана в цену.',
  badYear: 'Год был плохой, и хлеб это знает.',
  goodYear: 'Год был щедрый: хлеб дешёв, и потому дёшево всё, что им кормится.',
  toll: 'Город берёт своё с каждого воза.',
  risk: 'Дорога опасна, и возчик берёт за страх.',
} as const
