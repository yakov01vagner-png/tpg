/**
 * Путь торга и путь силы (этап 133) — содержимое.
 *
 * Два пути, у которых общее только одно: оба обходятся без войска. Торг делает
 * то, что делает войско, серебром — заёмом, откупом и наймом; сила делает то же
 * рангом, с которым корона говорит как с короной. И у каждого своя цена: торг
 * платится годами, сила — тем, что перестаёшь быть только человеком.
 */

/** Чем серебро делает работу войска. */
export const LEVERS = ['loan', 'buyPeace', 'hire'] as const
export type LeverId = (typeof LEVERS)[number]

export const LEVER_DEFS: Record<
  LeverId,
  { readonly label: string; readonly about: string; readonly after: string }
> = {
  loan: {
    label: 'заём короне',
    about: 'Дать в долг тому, кто воюет: должник не идёт на заимодавца.',
    after: 'Пока долг не отдан, с тобой говорят иначе — и признают.',
  },
  buyPeace: {
    label: 'откуп от войны',
    about: 'Заплатить за то, чтобы война кончилась сегодня, а не через год.',
    after: 'Откупившийся не выглядит победителем, зато не теряет людей.',
  },
  hire: {
    label: 'наём чужих войск',
    about: 'Роты (этап 86) воюют за того, кто платит: своих людей можно не класть.',
    after: 'Наёмное войско уходит, когда кончается серебро.',
  },
}

export const LEVER = {
  /** Сколько дней в году долг прирастает — доля. */
  interest: 0.0004,
  /** Наименьший заём, о котором вообще говорят с короной. */
  leastLoan: 4000,
  /** Во сколько раз откуп дороже годовой дани. */
  buyPeaceTimes: 2.4,
  /** Насколько заём поднимает отношение. */
  loanWarms: 10,
  /** Сколько серебра считается «домом, который кормит королевства». */
  feedsKingdoms: 120000,
} as const

/** Ступени силы: чем выше, тем меньше в тебе человека. */
export const MIGHT = {
  /** С какой ступени корона считается с магом как с короной. */
  crownReckons: 5,
  /** Сколько артефактов делает силу видимой. */
  artifacts: 3,
  /** Насколько церковь злится на высокий ранг за такт. */
  churchFears: 3,
  /** Раз во сколько суток считается цена силы. */
  beat: 30,
  /** Насколько высокий ранг отдаляет от людей: доля к верности вассалов. */
  apart: -2,
  /** Во сколько войска короны оценивают ранг, с которым считаются. */
  fearWorth: 1200,
} as const

export const LEVER_WORDS = {
  feeds: 'Дом, который кормит королевства: без твоего серебра мир считается иначе.',
  owes: 'Корона в долгу говорит не так, как корона свободная.',
  bought: 'Война кончена деньгами: люди целы, слава не выросла.',
  reckons: 'С тобой считаются как с короной — без земли и без войска.',
  apart: 'Ранг отдаляет: чем выше ступень, тем меньше в тебе человека, и это видят все.',
  fears: 'Церковь не любит тех, кто умеет то, чего не умеет она.',
  banker: 'Мир с одним заимодавцем: войны кончаются раньше, а долги переживают государей.',
  archmage: 'Мир с одним архимагом: короны считаются с ним, а люди обходят его стороной.',
} as const
