/**
 * Старость (этап 189) — содержимое.
 *
 * Годы в игре отнимают: сила падает, выносливость падает, потолки опускаются
 * (0.8). Не прибавляют ничего. Оттого старость была наказанием за то, что
 * долго играешь, а не временем, в котором играют иначе. Здесь лежит то, что
 * годы дают.
 */

export const ELDER = {
  /** С какого возраста слово весит больше удара. */
  weighsFrom: 50,
  /** Насколько слово тяжелее за каждый год сверх. */
  perYear: 0.02,
  /** Предел прибавки к слову. */
  most: 1.6,
  /** С какого возраста в поход не ходят. */
  noWarFrom: 62,
  /** Сколько лет наследник считается готовым принять дела. */
  heirReady: 18,
  /** Насколько отход от дел поднимает лад в доме. */
  retireCalm: 12,
} as const

export const ELDER_WORDS = {
  weighs: 'Слово твоё весит больше удара: с тобой говорят как со старшим.',
  body: 'Тело уже не то, и это знают все — потому и не зовут в поход.',
  heir: 'Наследник вырос: последние годы — о том, кому и что достанется.',
  retire: 'Отойти от дел можно: остаться в мире советчиком, а не хозяином.',
  late: 'Времени мало, и всякое дело теперь меряется тем, успеешь ли.',
} as const
