/**
 * Продвижение и гонка (этап 141) — содержимое.
 *
 * Путь у корон появился (этап 140), но пока он был тихим числом, гонки не
 * было: никто никому не мешал и никто никуда не спешил. Здесь у продвижения
 * появляются события, соперники, спешка у конца и откат.
 */

/** Что тормозит идущего (Гн4). */
export const DRAGS = ['league', 'fronts', 'land'] as const
export type DragId = (typeof DRAGS)[number]

export const DRAG_DEFS: Record<DragId, { readonly label: string; readonly says: string }> = {
  league: {
    label: 'коалиция',
    says: 'Против него сошлись: пока воюют все разом, идти вперёд нечем.',
  },
  fronts: {
    label: 'война на два фронта',
    says: 'Две войны сразу: войско в поле, а путь стоит.',
  },
  land: {
    label: 'потеря земли',
    says: 'Потерянное место откатывает путь: взятое надо было удержать.',
  },
}

export const RACE = {
  /** Насколько должна вырасти доля, чтобы это считалось шагом вперёд. */
  step: 0.04,
  /** С какой доли пути идущий начинает рисковать. */
  risksAt: 0.7,
  /** Раз во сколько суток считается гонка. */
  beat: 30,
  /** Насколько рискующий теряет верность своих за такт. */
  risksLoyalty: -3,
  /** И насколько холодеет к нему мир за нарушенное слово. */
  risksWord: -4,
  /** Насколько двое на одном пути мешают друг другу: доля к шагу. */
  sameWay: 0.7,
} as const

export const RACE_WORDS = {
  step: 'Шаг вперёд заметен: чужое продвижение — событие, а не тихое число.',
  rivals: 'Двое на одном пути мешают друг другу: то, что взял один, не возьмёт другой.',
  risks: 'Близко к концу рискуют больше: берут займы, нарушают слово, бросают своих.',
  drag: 'Путь откатывается: коалиция, война на два фронта и потеря земли — всё это назад.',
  leads: 'Кто ведёт гонку, видно всегда — но с вилкой: чужое продвижение ты знаешь вестями.',
} as const
