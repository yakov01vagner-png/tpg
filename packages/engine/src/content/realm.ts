/**
 * Закон державы (этап 76) — содержимое.
 *
 * Закон 0.6 был законом владения: подать, мыто, повинность и суд — на своей
 * земле и для своего управляющего. Держава меняет в нём не числа, а охват:
 * теперь то же слово стоит над вассальными землями и над городами, у которых
 * есть чем ответить.
 *
 * Здесь лежит то, что у этого авторского: вольности городов, ответы на
 * недоимку и слова годового отчёта.
 */

/** Вид грамоты: город просит вольность, место — послабление. */
export const CHARTER_KINDS = ['liberty', 'relief'] as const
export type CharterKind = (typeof CHARTER_KINDS)[number]

export const CHARTER_DEFS: Record<
  CharterKind,
  {
    readonly label: string
    readonly about: string
    /** Какую долю подати ты после этого берёшь. */
    readonly take: number
    /** Насколько место довольнее. */
    readonly mood: number
  }
> = {
  liberty: {
    label: 'вольность',
    about:
      'Город судит сам, держит свою стражу и платит не подать, а договорный взнос. Взамен даёт деньги сразу и помнит это долго.',
    take: 0.4,
    mood: 25,
  },
  relief: {
    label: 'послабление',
    about: 'Подать снижена на срок: так договариваются с теми, кто и правда не может платить.',
    take: 0.6,
    mood: 12,
  },
}

/** На сколько суток даётся послабление. */
export const RELIEF_DAYS = 180

/** С какого населения город вообще заговаривает о вольности. */
export const LIBERTY_POPULATION = 4000

/**
 * Сколько город платит за вольность: с души, разом.
 *
 * Шесть десятых — это примерно два месяца той подати, которую ты с него больше
 * не возьмёшь. Меньше — и вольность никто не купит; больше — и её выгодно
 * раздавать всем подряд, а это уже не выбор.
 */
export const LIBERTY_PRICE = 0.6

/** Ниже этой доли дошедшей подати место считается недоимщиком. */
export const DEBTOR_FACTOR = 0.8

/** Чем отвечают на недоимку (З3). */
export const ARREARS_ANSWERS = ['force', 'deal', 'forgive'] as const
export type ArrearsAnswer = (typeof ARREARS_ANSWERS)[number]

export const ARREARS_DEFS: Record<
  ArrearsAnswer,
  {
    readonly label: string
    readonly about: string
    /** Какую долю недоимки берёшь сейчас. */
    readonly takes: number
    /** Как это меняет память места. */
    readonly mood: number
    /** И разбой вокруг. */
    readonly banditry: number
  }
> = {
  force: {
    label: 'Взять силой',
    about: 'Приехать с людьми и забрать. Возьмёшь всё, а помнить будут это дольше, чем цифру.',
    takes: 1,
    mood: -14,
    banditry: 0.08,
  },
  deal: {
    label: 'Договориться',
    about: 'Взять половину и дать послабление на полгода. Так делают с теми, кого хотят сохранить.',
    takes: 0.5,
    mood: 6,
    banditry: -0.02,
  },
  forgive: {
    label: 'Простить',
    about: 'Не брать ничего. Дорого стоит и помнится дольше всего.',
    takes: 0,
    mood: 18,
    banditry: -0.05,
  },
}

/** Слова годового отчёта (З6). */
export const YEAR_WORDS = {
  rich: 'Год прибыльный: казна прибавила.',
  thin: 'Год впритык: сошлось, и только.',
  poor: 'Год убыточный: держава ест больше, чем приносит.',
  quiet: 'Недовольных нет.',
  grumbling: 'Есть недовольные, и их слышно.',
  dangerous: 'Недовольство такое, что до мятежа ближе, чем до следующей жатвы.',
} as const
