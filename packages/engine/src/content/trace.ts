/**
 * Мир после тебя (этап 148) — содержимое.
 *
 * Смерть государя не кончает мир: он считается тем же тактом, что и вчера.
 * Наследнику достаётся всё — земля, грамоты, признания и долги, — а через
 * двадцать лет видно, что из этого осталось.
 */

/** Что остаётся от человека (Пс3). */
export const LEAVES = ['land', 'papers', 'kin', 'debts', 'name'] as const
export type LeaveId = (typeof LEAVES)[number]

export const LEAVE_DEFS: Record<LeaveId, { readonly label: string; readonly says: string }> = {
  land: { label: 'земля', says: 'Места, которые он взял и которые ещё держат его потомки.' },
  papers: {
    label: 'грамоты',
    says: 'Договоры и поручительства, которые пережили того, кто их подписал.',
  },
  kin: {
    label: 'родство',
    says: 'Браки, которыми он связал дом: родство переходит к наследникам.',
  },
  debts: { label: 'долги', says: 'Чужие долги ему и его долги чужим: серебро переживает людей.' },
  name: { label: 'имя', says: 'То, что о нём помнят: имя живёт дольше земли.' },
}

export const TRACE = {
  /** Через сколько лет после смерти смотрят, что осталось. */
  afterYears: 20,
  /** Во сколько раз дом помнят дольше человека. */
  houseLonger: 2,
} as const

export const TRACE_WORDS = {
  goes: 'Мир идёт дальше: он считается тем же тактом, каким считался при тебе.',
  got: 'Наследнику досталось всё: и хорошее, и долги.',
  left: 'Через двадцать лет видно, что от тебя осталось.',
  fell: 'Род пресёкся: мир это переживёт, но запомнит.',
  house: 'Короны помнят дом дольше, чем человека: имя живёт, пока живёт память.',
} as const
