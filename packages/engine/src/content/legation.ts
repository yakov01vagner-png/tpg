/**
 * Посольство как люди (этап 193) — содержимое.
 *
 * Посол с 0.8 — человек с нравом и умением, и этим дело кончается: дорога
 * ничего не стоит, приём ничего не значит, резидент не стареет и не обрастает
 * связями, а чужое посольство у тебя — строка с ответом. Здесь лежит то, из
 * чего посольство делается людьми.
 */

export const WELCOMES = ['honour', 'plain', 'cold', 'shut'] as const
export type WelcomeId = (typeof WELCOMES)[number]

export const WELCOME_DEFS: Record<
  WelcomeId,
  { readonly label: string; readonly costs: number; readonly mood: number; readonly says: string }
> = {
  honour: {
    label: 'с честью',
    costs: 900,
    mood: 12,
    says: 'Встретили за версту, поставили в лучшем доме, кормили три дня.',
  },
  plain: {
    label: 'по чину',
    costs: 250,
    mood: 4,
    says: 'Приняли как принимают: без обид и без радости.',
  },
  cold: {
    label: 'холодно',
    costs: 40,
    mood: -8,
    says: 'Подержали у ворот, говорили стоя. Это заметят.',
  },
  shut: {
    label: 'не приняли',
    costs: 0,
    mood: -20,
    says: 'Ворота не открыли. Такое помнят дольше войны.',
  },
}

export const LEGATION = {
  /** Сколько суток идёт посольство за переход. */
  perHop: 2,
  /** Во сколько обходится содержание посольства в сутки. */
  perDay: 18,
  /** Насколько дорога портит посла: доля к его умению за каждые десять суток. */
  roadWears: 0.04,
  /** Сколько лет резидент сидит, прежде чем обрастёт связями. */
  rootsYears: 3,
  /** Насколько связи поднимают, что он узнаёт. */
  rootsSee: 1.5,
  /** И насколько растёт шанс, что его раскроют. */
  rootsRisk: 0.08,
} as const

export const LEGATION_WORDS = {
  road: 'Дорога портит посла: он приезжает не тем, кем выехал.',
  welcome: 'Приём — половина посольства: с чем встретили, с тем и уедет.',
  roots: 'Резидент оброс связями: видит больше и рискует больше.',
  brings: 'Посол привозит ответ, картину чужого двора и свои домыслы.',
  theirs: 'Чужое посольство — такая же сцена, как своё.',
} as const
