/**
 * Случайность, которую видно (этап 207) — содержимое.
 *
 * У генератора один шаг: состояние каждый раз сдвигается на одно и то же
 * число. Значит, по двум состояниям можно узнать, сколько бросков между ними
 * сделано, — и счёт бросков перестаёт быть догадкой.
 *
 * Здесь лежат то самое число, обратное к нему и перечень мест, где случайности
 * нет нарочно.
 */

/** Шаг генератора (mulberry32): на столько сдвигается состояние за бросок. */
export const ROLL_STEP = 0x6d2b79f5

/**
 * Обратное к шагу по модулю 2³².
 *
 * `ROLL_STEP * ROLL_INVERSE ≡ 1 (mod 2³²)`, поэтому разность двух состояний,
 * умноженная на него, и есть число бросков между ними.
 */
export const ROLL_INVERSE = 0xdc58aa5d

/**
 * Где ответ берётся хэшем, а не броском (Сл4).
 *
 * Правило простое: если на один и тот же вопрос мир обязан отвечать одинаково
 * всякий раз, когда его спросят, — бросок не годится. Бросок расходует поток и
 * потому зависит от того, сколько раз его спросили; хэш — нет.
 */
export const HASHED = [
  { what: 'дом короны', why: 'кто сидит на троне, не меняется от того, сколько раз спросили' },
  { what: 'двор', why: 'один и тот же двор при одном и том же колене' },
  { what: 'связи при дворе', why: 'дружбы и вражды выводятся из людей, а не выпадают' },
  { what: 'купцы и мастера в месте', why: 'кто торгует в городе, не зависит от порядка захода' },
  { what: 'раскусили ли обман', why: 'один и тот же взгляд на один и тот же обман видит одно' },
  { what: 'ошибка картины мира', why: 'корона держится своего заблуждения, а не бросает заново' },
] as const

export const ROLL_WORDS = {
  one: 'Поток один на мир, и он лежит в состоянии: своей случайности у слоёв нет.',
  counted: 'Броски считаны: их число выводится из самого генератора.',
  quiet: 'Новый слой в единице не двигает чужую случайность — старые прогоны повторяются.',
  hashed: 'Где ответ обязан быть одним и тем же, стоит хэш, а не бросок.',
} as const
