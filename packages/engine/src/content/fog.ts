/**
 * Туман войны (этап 109) — содержимое.
 *
 * Война 0.7 считалась по полному знанию карты: каждая чужая дружина была видна
 * всем и всегда, где бы ни стояла. Оттого война была задачей на счёт, а не на
 * решение: зная всё, воевать просто. Здесь у чужого войска появляется место, где
 * его в последний раз видели, — и круг, в котором оно может быть теперь.
 */

/** Чей глаз что видит. */
export const EYES = ['place', 'host', 'trader', 'peasant'] as const
export type EyeKind = (typeof EYES)[number]

export const EYE_DEFS: Record<
  EyeKind,
  {
    readonly label: string
    readonly about: string
    /** На сколько переходов видит. */
    readonly hops: number
    /** Как быстро доносит: суток задержки на переход. */
    readonly delay: number
  }
> = {
  place: {
    label: 'своё место',
    about: 'Город видит, кто прошёл мимо его стен, и посылает сказать.',
    hops: 2,
    delay: 1,
  },
  host: {
    label: 'своя часть',
    about: 'Войско в поле видит дальше всех: у него есть кому ездить вперёд.',
    hops: 4,
    delay: 0.5,
  },
  trader: {
    label: 'торговый человек',
    about: 'Купец ходит дорогами и говорит о том, что видел, — за плату или так.',
    hops: 3,
    delay: 2,
  },
  peasant: {
    label: 'местные',
    about: 'Округа, которая тебя любит, рассказывает сама. Нелюбящая молчит.',
    hops: 3,
    delay: 3,
  },
}

export const FOG = {
  /** На сколько переходов расплывается место войска за сутки без вестей. */
  driftPerDay: 0.22,
  /** Шире этого круга искать бессмысленно: войско считается пропавшим. */
  maxDrift: 5,
  /** Через сколько суток весть о войске уже ничего не значит. */
  staleDays: 40,
  /** Раз во сколько суток собираются донесения дозорных глаз. */
  beat: 3,
  /** Насколько любит округа, чтобы местные вообще заговорили. */
  peasantRep: 15,
  /** Сколько духа теряет тот, кого застали врасплох. */
  surpriseMorale: 14,
  /** С какого возраста вести войско считается «неизвестно где». */
  lostAfter: 12,
} as const

export const FOG_WORDS = {
  never: 'Об этом войске ты не слышал вовсе.',
  here: 'Ты видишь его сам.',
  lost: 'Где оно теперь — неизвестно.',
  surprise: 'Их не ждали: строй ломается прежде первого удара.',
  ready: 'Их ждали: встали как надо.',
  theirs: 'Твои части они видят так же плохо, как ты — их.',
} as const
