/**
 * Признание (этап 138) — содержимое.
 *
 * Титул в 0.6 был тем, чем ты владеешь: доросла земля — стал герцогом. Это
 * половина правды. Вторая половина в том, что титул значит ровно столько,
 * сколько его признают: коронация, на которую никто не приехал, — слово без
 * дела. Здесь у признания появляется цена, отзыв и то, чего стоит жить без него.
 */

/** Сколько признаний нужно, чтобы титул что-нибудь значил (Пр1). */
export const TITLE_NEEDS: Readonly<Record<string, number>> = {
  freeholder: 0,
  baron: 0,
  count: 1,
  duke: 3,
  king: 5,
}

/** Чем дороже жизнь непризнанного (Пр4). */
export const DEARER = ['envoys', 'allies', 'hire', 'weddings'] as const
export type DearerId = (typeof DEARER)[number]

export const DEARER_DEFS: Record<DearerId, { readonly label: string; readonly says: string }> = {
  envoys: {
    label: 'послы',
    says: 'Непризнанного принимают не сразу и не всякий: посольство идёт дольше и стоит дороже.',
  },
  allies: {
    label: 'союзы',
    says: 'С непризнанным не встают рядом: союз с самозванцем — половина союза.',
  },
  hire: {
    label: 'наём',
    says: 'Роты берут с непризнанного вперёд и больше: платить ему может стать нечем.',
  },
  weddings: {
    label: 'браки',
    says: 'За непризнанного отдают хуже и просят больше: приданое считается по титулу.',
  },
}

export const ACCLAIM = {
  /** Насколько дороже всё тому, кого не признали, — за каждого непризнавшего. */
  perStranger: 0.08,
  /** Дороже этого не становится. */
  dearestTimes: 1.6,
  /** Раз во сколько суток короны решают, не отозвать ли признание. */
  beat: 30,
  /** Насколько холодное отношение ведёт к отзыву. */
  recallsAt: -45,
  /** Насколько отзыв роняет отношение к отзывающему. */
  recallChills: -20,
} as const

export const ACCLAIM_WORDS = {
  hollow: 'Титул, которого не признали, — слово без дела: называться можно как угодно.',
  worth: 'Титул значит то, что его признали: столько, сколько за ним стоит корон.',
  recalled: 'Отозванное признание — повод к войне, и мир понимает это именно так.',
  stranger: 'Непризнанному дороже всё: послы, союзы, наём и браки.',
  given: 'Признать чужого — не любезность: этим ты двигаешь его путь.',
} as const
