/**
 * Коалиция против первого (этап 136) — содержимое.
 *
 * Коалиции были и в 0.7 (этап 91), но складывались они по силе: у кого войска
 * больше, против того и сходятся. История складывала их иначе — против того,
 * кто вот-вот дойдёт. Здесь у коалиции появляется та причина, которой не было,
 * и вместе с ней — цена, за которую её можно разобрать.
 */

/** Чем разбирают коалицию: у каждого свой ключ (Ко3). */
export const KEYS = ['coin', 'yield', 'kin', 'secret'] as const
export type KeyId = (typeof KEYS)[number]

export const KEY_DEFS: Record<
  KeyId,
  { readonly label: string; readonly about: string; readonly after: string }
> = {
  coin: {
    label: 'выкуп',
    about: 'Деньги вперёд: тому, кто вошёл в коалицию за добычей, добыча и нужна.',
    after: 'Купленный выходит тихо и в следующий раз просит дороже.',
  },
  yield: {
    label: 'уступка',
    about: 'Отдать спорное: землю, дань или право — тому, кто вошёл за обиду.',
    after: 'Уступка помнится дольше выкупа: отданное назад не просят.',
  },
  kin: {
    label: 'брак',
    about: 'Родство вместо войны: родня в коалицию против родни не ходит.',
    after: 'Брак держит, пока жив тот, кем он заключён.',
  },
  secret: {
    label: 'тайная статья',
    about: 'Обещание в сторону: тому, кто боится не тебя, а соседа.',
    after: 'Тайное держит, пока о нём не узнали прочие.',
  },
}

export const LEAGUE = {
  /** Сколько корон должно бояться, чтобы коалиция сложилась. */
  least: 3,
  /** Раз во сколько суток мир пересчитывает, против кого сходиться. */
  beat: 20,
  /** Сколько суток коалиция держится, даже если страх упал. */
  holds: 360,
  /** Во что обходится выкуп одного участника — за каждое его место. */
  coinPerPlace: 320,
  /** Насколько уступка греет того, кому уступили. */
  yieldWarms: 25,
  /** Во что обходится созвать коалицию против чужого: серебро. */
  callCost: 9000,
  /** И сколько это берёт часов. */
  callHours: 40,
  /** Насколько участие в коалиции холодит отношение к цели. */
  chills: -25,
} as const

export const LEAGUE_WORDS = {
  why: 'Сходятся не против сильного, а против того, кто вот-вот дойдёт.',
  bound:
    'Связанный с тобой не идёт против тебя: родня, данник, должник и союзник остаются в стороне.',
  uneven: 'Война всех против одного идёт несогласованно: каждый воюет свою войну и мирится сам.',
  bought: 'Коалиция разбирается по одному: у всякого своя цена и свой ключ.',
  called: 'Ты собрал мир против того, кто ближе тебя к концу: теперь добыча — он.',
  none: 'Никто ни против кого не сходится.',
} as const
