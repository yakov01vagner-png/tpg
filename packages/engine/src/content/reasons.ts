/**
 * Решение, которое объясняется (этап 197) — содержимое.
 *
 * Корона решает с этапа 72, и у решения с самого начала была строка «почему».
 * Но строка писалась рядом с решением, а не была им: нигде не сказано, какое
 * правило сработало, какие не сработали и почему. Здесь лежат правила — те
 * самые, по порядку, в каком их читает `plans.ts`.
 */

export const CROWN_RULES = [
  'atWar',
  'owes',
  'fearsGiant',
  'seeksHelp',
  'holds',
  'weds',
  'quiet',
] as const
export type CrownRuleId = (typeof CROWN_RULES)[number]

export interface CrownRuleDef {
  readonly id: CrownRuleId
  readonly label: string
  /** Что корона делает, когда правило срабатывает. */
  readonly does: string
  /** Почему это правило стоит именно здесь в череде. */
  readonly before: string
}

export const CROWN_RULE_DEFS: Record<CrownRuleId, CrownRuleDef> = {
  atWar: {
    id: 'atWar',
    label: 'война уже идёт',
    does: 'ведёт начатую и второй не открывает',
    before: 'Начатая война — уже выбор: пока она жива, прочее не решается.',
  },
  owes: {
    id: 'owes',
    label: 'платит дань',
    does: 'ищет, как сбросить дань',
    before: 'Дань тяжелее войны: платящий думает о ней прежде всего.',
  },
  fearsGiant: {
    id: 'fearsGiant',
    label: 'боится выросшего',
    does: 'выбирает врагом того, кто забрал слишком много',
    before: 'Воинственный идёт на выросшего сам, не дожидаясь союзников.',
  },
  seeksHelp: {
    id: 'seeksHelp',
    label: 'ищет союза против выросшего',
    does: 'ищет союзника против того, кто забрал слишком много',
    before: 'Кто не воинствен, тот против выросшего в одиночку не встаёт.',
  },
  holds: {
    id: 'holds',
    label: 'сама выросла',
    does: 'держит взятое',
    before: 'Взявшему много не до нового: удержать бы это.',
  },
  weds: {
    id: 'weds',
    label: 'давно в ладу',
    does: 'говорит о наследниках, а не о войне',
    before: 'С кем давно в ладу, с тем говорят о браке.',
  },
  quiet: {
    id: 'quiet',
    label: 'ничего не затевает',
    does: 'держит взятое и ждёт',
    before: 'Год без войны — тоже дело.',
  },
}

export const CROWN_REASONS = {
  /** Насколько воинственной надо быть, чтобы идти на выросшего одной. */
  lustGoesAlone: 1.2,
  /** Отношение, выше которого говорят о браке. */
  warmWeds: 20,
} as const

export const REASON_WORDS = {
  named: 'У всякого хода есть правило, по которому он сделан, и оно называется.',
  chain: 'Ход — не сам по себе: он шаг долгой партии.',
  refused: 'Отказ объясняется так же, как согласие: не сработало — сказано, почему.',
  honest: 'Названная причина — та самая, по которой решение принято: правило одно на обоих.',
  seen: 'Это ты знаешь, а не угадываешь: есть кому подслушать.',
  guessed: 'Это догадка: подслушать некому.',
} as const
