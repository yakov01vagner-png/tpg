/**
 * Чужие договоры (этап 116) — содержимое.
 *
 * До 0.8 чужой союз был записью в состоянии: игрок видел его сразу и целиком.
 * Здесь между «знать» и «доказать» появляется расстояние, и оно оказывается
 * главным в дипломатии: мир верит не тому, кто прав, а тому, у кого бумага.
 */

export const PROOFS = ['letter', 'witness', 'seal', 'forged'] as const
export type ProofKind = (typeof PROOFS)[number]

export const PROOF_DEFS: Record<
  ProofKind,
  {
    readonly label: string
    readonly about: string
    /** Насколько ему верят третьи: 0..1. */
    readonly weight: number
    readonly cost: number
    /** Что нужно, чтобы добыть. */
    readonly needs: string
  }
> = {
  letter: {
    label: 'перехваченное письмо',
    about: 'Чужая рука, чужая печать, чужие слова. Верят, если печать та.',
    weight: 0.6,
    cost: 600,
    needs: 'свой человек при их дворе',
  },
  witness: {
    label: 'живой свидетель',
    about: 'Тот, кто был при разговоре. Он может и передумать.',
    weight: 0.45,
    cost: 900,
    needs: 'третья корона, при которой договаривались',
  },
  seal: {
    label: 'сама грамота',
    about: 'Лист с двумя печатями. Спорить с этим нельзя.',
    weight: 0.85,
    cost: 2200,
    needs: 'кража или измена в их канцелярии',
  },
  forged: {
    label: 'подделка',
    about: 'Похоже на правду настолько, насколько хорош твой писец. Пока не сличили.',
    weight: 0.7,
    cost: 700,
    needs: 'свой писец и немного совести',
  },
}

export const PROOF = {
  /** Насколько слово без бумаги вообще кого-то трогает. */
  bareWord: 0.15,
  /** Насколько доказательство весит на съезде против простого слова. */
  atCongress: 0.55,
  /** Насколько ветшает доказательство за год. */
  agesPerYear: 0.18,
  /** Насколько легко сличить подделку тому, у кого есть свои глаза. */
  forgerySeen: 0.3,
  /** Чего стоит раскрытая подделка: гнев всех, кто поверил. */
  forgeryCost: -40,
  /** Насколько поворачивает третьих раскрытый чужой сговор. */
  turnsThirds: 22,
} as const

export const PROOF_WORDS = {
  known: 'Ты знаешь. Этого мало.',
  got: 'Доказательство добыто.',
  shown: 'Предъявлено миру.',
  bare: 'Без бумаги это твоё слово против их слова, и мир пожимает плечами.',
  forged: 'Писец сделал, что мог.',
  caught: 'Подделку сличили. Теперь врун — ты.',
  congress: 'На съезде бумага весит больше речи.',
  stale: 'Бумага стара: с тех пор всё могло перемениться.',
} as const
