/**
 * Война, которая кончается по счёту (этап 163) — содержимое.
 *
 * До 1.0 война кончалась броском: 0.006 в сутки, поделённые на упрямство повода
 * и на воинственность коронм. Оттого сто войн за век выглядели одинаково —
 * объявили, помирились, всё как было, — и ни одна не кончалась потому, что
 * добилась своего или что больше не может. Здесь лежат веса счёта, которым
 * бросок теперь взвешивается.
 */

export const TALLY = {
  /**
   * Раз во сколько суток сводится счёт войны.
   *
   * Война не меняется за сутки: взятая марка, усталость и чужое давление —
   * вещи недельного порядка. Считать их каждый день значит платить за то, что
   * не сдвинулось: такт войны идёт по всем войнам мира разом.
   */
  beat: 5,
  /** За сколько лет война выматывает обе стороны дотла. */
  wearyYears: 5,
  /** Во сколько раз охотнее мирится вымотанный. */
  wearyHaste: 4,
  /** Во сколько раз охотнее мирится тот, кто своего добился. */
  reachedHaste: 6,
  /** И тот, кому своего уже не добиться. */
  hopelessHaste: 3.5,
  /** Со скольких лет война считается безнадёжной, если не сдвинулась. */
  hopelessYears: 3,
  /** Насколько сильнее надо быть, чтобы считать, что можешь добить. */
  beatenBy: 1.5,
  /** Во сколько раз охотнее мирятся под чужим давлением (за каждую долю). */
  outsideHaste: 2,
  /** Что даёт посредник — третий, с кем в союзе оба. */
  mediator: 0.5,
  /** Что даёт поручительство за одну из сторон. */
  guarantee: 0.35,
  /** И сходящаяся против одной из сторон коалиция. */
  coalition: 0.4,
  /** Во что обходится первая зима войны — серебром в сутки на сторону. */
  winterCost: 120,
  /** Во сколько раз дорожает каждая следующая. */
  winterGrows: 1.6,
  /** Какой долей земли надо держать спорную провинцию, чтобы счесть её взятой. */
  holdsProvince: 0.6,
} as const

export const TALLY_WORDS = {
  reached: 'Своего добились: дальше воюют по привычке, а не за цель.',
  hopeless: 'Цели не взять: войну тянут, чтобы не признать этого первыми.',
  weary: 'Обе стороны выдохлись: такие войны кончаются ничем.',
  outside: 'На них давят со стороны: войну кончают не они.',
  fresh: 'Война молода, цель не взята и не потеряна: мириться рано.',
} as const
