/**
 * Съезд, который решает (этап 194) — содержимое.
 *
 * Съезд с 0.8 есть: повестка, голоса, подкуп, счёт. Чего нет — объяснения:
 * голос выходит числом, торг вокруг решения сводится к подкупу, а решённое
 * ничем не держится. Здесь лежит то, чем съезд делается съездом.
 */

export const DIET_MOVES = ['bribe', 'swap', 'threat', 'ally', 'proof'] as const
export type DietMove = (typeof DIET_MOVES)[number]

export const DIET_MOVE_DEFS: Record<
  DietMove,
  { readonly label: string; readonly about: string; readonly weight: number; readonly risk: string }
> = {
  bribe: {
    label: 'подкуп',
    about: 'Серебро в рукав: дёшево и сердито, пока не всплыло.',
    weight: 0.8,
    risk: 'всплывёт — потеряешь больше, чем купил',
  },
  swap: {
    label: 'обмен',
    about: 'Его голос за твой в другом деле. Самая честная из нечестных сделок.',
    weight: 0.6,
    risk: 'придётся отдавать, и отдавать при всех',
  },
  threat: {
    label: 'угроза',
    about: 'Не за тебя — значит против; такое запоминают.',
    weight: 0.7,
    risk: 'обиженный голосует против даже себе в убыток',
  },
  ally: {
    label: 'союзник',
    about: 'Пусть скажет тот, кому верят больше, чем тебе.',
    weight: 0.5,
    risk: 'союзник попросит своё — потом',
  },
  proof: {
    label: 'доказательство',
    about: 'Бумага на столе: на съезде она весит больше речи.',
    weight: 1,
    risk: 'подложная бумага — конец твоему слову',
  },
}

export const DIET = {
  /** Сколько суток идёт съезд. */
  days: 20,
  /** Сколько голосов нужно сверх половины. */
  needs: 1,
  /** Насколько решённое связывает: цена нарушения в отношениях. */
  binds: -25,
  /** Сколько суток решённое держится, прежде чем о нём забывают. */
  holds: 1095,
} as const

export const DIET_WORDS = {
  agenda: 'Повестку задаёт мир, а не хозяин съезда.',
  why: 'У всякого голоса есть причина, и она не в симпатии.',
  bargain: 'Торг идёт не на съезде, а вокруг него.',
  binds: 'Решённое связывает: нарушил — и об этом помнят все, кто там был.',
  scene: 'Съезд читается: кто приехал, кто о чём говорил и чем кончилось.',
} as const
