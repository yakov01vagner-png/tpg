/**
 * Повтор (этап 206) — содержимое.
 *
 * Своих величин у повтора почти нет: он ничего не решает в мире, он только
 * сверяет. Здесь лежит то немногое, что всё же выбрано рукой.
 */

export const REPLAY = {
  /** Сколько знаков значения показывать в расхождении: хватает, чтобы узнать. */
  shows: 60,
  /** Длина цепи, на которой повтор считается доказанным. */
  chain: 100,
  /** На каком ходу цепь пишется и поднимается заново. */
  savesAt: 50,
} as const

export const REPLAY_WORDS = {
  held: 'Повтор держится: из одного сейва выходит один мир.',
  broke: 'Повтор разошёлся.',
  clock: 'Часы машины в мир не входят: ни одного решения по времени снаружи.',
} as const
