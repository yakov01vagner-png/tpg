/**
 * Потеря державы (этап 151) — содержимое.
 *
 * До сих пор потеря последнего места означала, что играть больше нечем. Но
 * человек не кончается вместе с державой: у него остаются имя, дом, слово,
 * спутники и знание — и с этого можно начать заново или не начинать вовсе.
 */

/** Что остаётся, когда земли нет (По2). */
export const REMAINS = ['name', 'house', 'word', 'people', 'lore'] as const
export type RemainId = (typeof REMAINS)[number]

export const REMAIN_DEFS: Record<RemainId, { readonly label: string; readonly says: string }> = {
  name: {
    label: 'имя',
    says: 'Имя и то, что о нём помнят: отнять это нельзя, можно только забыть.',
  },
  house: { label: 'дом', says: 'Род и родство: дом стоит, пока жив хоть один.' },
  word: { label: 'слово', says: 'Грамоты и поручительства: их не отменяет потеря земли.' },
  people: { label: 'спутники', says: 'Те, кто остался при тебе, когда осталось только это.' },
  lore: { label: 'знание', says: 'Умения и книги: единственное, что нельзя забрать войском.' },
}

export const FALLEN = {
  /** Насколько бывшие вассалы холодеют к потерявшему землю. */
  vassalsChill: -25,
  /** Насколько холодеет мир. */
  worldChill: -10,
  /** Насколько тепло должно быть, чтобы приютили изгнанника. */
  exileWarmth: 10,
  /** Раз во сколько суток смотрят, цела ли держава. */
  beat: 10,
} as const

export const FALLEN_WORDS = {
  lost: 'Держава кончилась: последнее место взято. Ты — нет.',
  goes: 'Игра не кончена: без земли живут, и с этого начинали.',
  exile: 'Изгнанник живёт при чужом дворе: место есть, власти нет.',
  remember: 'Помнят по-разному: свои — с обидой, чужие — с расчётом.',
} as const
