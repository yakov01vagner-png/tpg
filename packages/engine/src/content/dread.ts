/**
 * Кто чего боится (этап 135) — содержимое.
 *
 * До этой версии короны считали силу: у кого больше войска, тот и опасен. С
 * появлением путей (блок I) опасен другой — тот, кому осталось меньше всех. У
 * восьмого места войска больше, а страха меньше: войско можно разбить, а
 * пройденный путь — нет.
 */

/** Из чего складывается страх (Бо1). */
export const FEARS = ['share', 'need', 'near', 'cold'] as const
export type FearId = (typeof FEARS)[number]

export const FEAR_DEFS: Record<
  FearId,
  { readonly label: string; readonly about: string; readonly weight: number }
> = {
  share: {
    label: 'близость к концу',
    about: 'Сколько пройдено его пути: тот, кому осталось мало, опаснее сильного.',
    weight: 55,
  },
  need: {
    label: 'ты у него на дороге',
    about: 'Его конец считается через тебя: без твоего признания или без твоей земли он не дойдёт.',
    weight: 25,
  },
  near: {
    label: 'соседство',
    about: 'Дальнего боятся позже: до него ещё надо дойти, и не ему первому.',
    weight: 12,
  },
  cold: {
    label: 'холод между вами',
    about: 'Тёплому соседу прощают то, чего не прощают холодному.',
    weight: 8,
  },
}

export const DREAD = {
  /** С какого числа считается, что эта корона тебя боится. */
  scares: 45,
  /** Раз во сколько суток короны пересчитывают, кто им опасен. */
  beat: 20,
  /** Раз во сколько суток молва разносит чужое продвижение. */
  wordBeat: 30,
  /** Насколько молва преувеличивает продвижение: доля. */
  rumourOff: 0.3,
  /** Сколько переходов считается соседством. */
  nearHops: 8,
} as const

export const DREAD_WORDS = {
  counted: 'Страх здесь считается, а не чувствуется: у него есть число и есть причина.',
  notStrength: 'Боятся не сильного, а близкого к концу: войско разбивают, пройденный путь — нет.',
  blind: 'О твоём продвижении они знают вестями: с опозданием, с вилкой или вовсе не знают.',
  each: 'Короны считают тем же счётом и друг друга: не ты один идёшь к концу.',
} as const
