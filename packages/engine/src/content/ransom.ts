/**
 * Плен и выкуп (этап 150) — содержимое.
 *
 * Плен в 0.6 был односторонним: в плен попадали чужие лорды, и с ними решали,
 * что делать. Здесь плен становится общим правилом — в него попадают все, — а
 * выкуп из числа превращается в торг: у пленителя свой расчёт.
 */

/** Что можно взять с пленника, кроме серебра (Пл3). */
export const LEVERS = ['silver', 'yield', 'recognise', 'peace'] as const
export type RansomLeverId = (typeof LEVERS)[number]

export const LEVER_DEFS: Record<RansomLeverId, { readonly label: string; readonly about: string }> =
  {
    silver: { label: 'серебром', about: 'Обычный выкуп: цена и день, когда привезут.' },
    yield: { label: 'уступкой', about: 'Спорная земля или дань вместо денег: дороже для чести.' },
    recognise: { label: 'признанием', about: 'Его корона признаёт тебя: пленник дороже золота.' },
    peace: { label: 'миром', about: 'Война кончается на твоих условиях: плен стоит войны.' },
  }

export const RANSOM = {
  /** Насколько пленитель запрашивает больше расчётной цены. */
  asks: 1.6,
  /** И ниже какой доли расчётной не опустится. */
  least: 0.7,
  /** Насколько воюющий торгуется мягче: ему нужно серебро сейчас. */
  atWar: 0.85,
  /** Раз во сколько суток в плену что-нибудь случается. */
  beat: 30,
  /** Насколько вероятен побег за такт. */
  escape: 0.08,
  /** Сколько верности теряет вассал за каждый год, оставленный в плену. */
  forgotten: -8,
} as const

export const RANSOM_WORDS = {
  all: 'В плен попадают все: и чужие лорды, и свои, и ты сам.',
  haggle: 'Выкуп торгуют, а не считают: у пленителя свой расчёт и своя нужда.',
  lever: 'Пленник — рычаг: за него берут не только серебро.',
  left: 'Оставленный в плену помнит это, и двор помнит тоже.',
  escaped: 'Из плена бегут: не всякий и не сразу, но бегут.',
} as const
