/**
 * Подать и недовольство (этап 185) — содержимое.
 *
 * Подать с 0.7 берётся сама: в казну капает число, и всё, что с ним связано —
 * сборщики, утечка, недобор, — считается в разных местах и нигде не сходится.
 * Недовольство тоже живёт по частям: память мест, настроение черни, обиды
 * вассалов. Здесь лежит то, чем подать и недовольство считаются вместе.
 */

export const IMPOST = {
  /** Какую долю подати теряет сбор, когда берёт чужой человек. */
  leaksPlain: 0.08,
  /** И сколько — вороватый. */
  leaksThief: 0.25,
  /** Насколько недобор растёт, если место голодает. */
  hungerShort: 0.4,
  /** И если в округе разбой. */
  banditryShort: 0.5,
  /** Сколько недовольства даёт тяжёлая подать за такт. */
  heavyAnger: 6,
  /** И сколько снимает послабление. */
  reliefCalm: -8,
  /** Насколько голод поднимает недовольство. */
  hungerAnger: 9,
  /** И разбой. */
  banditryAnger: 5,
  /** И обиженный вассал. */
  sulkyAnger: 4,
  /** Насколько суд и подарки сбивают его. */
  justiceCalm: -4,
  /** С какого недовольства начинается мятеж. */
  risesAt: 60,
  /** Раз во сколько суток недовольство пересчитывается. */
  beat: 30,
} as const

export const IMPOST_WORDS = {
  leak: 'Сборщик берёт своё: до казны доходит не всё.',
  short: 'Недобор: с голодной земли не возьмёшь того, чего на ней нет.',
  heavy: 'Подать тяжела, и это видно по тому, как на тебя смотрят.',
  calm: 'Земля терпит: берут не больше, чем принято.',
  rises: 'Терпение кончилось. Это уже не ропот.',
  sides: 'В мятеже у каждого своя сторона, и она объяснима.',
} as const
