/**
 * Разговор вместо кнопки (этап 191) — содержимое.
 *
 * Дипломатия к 1.0 умеет почти всё: договоры, тайные статьи, свидетелей,
 * посольства, съезды, признание, гарантии, коалиции. Чего она не умеет — быть
 * разговором: всё делается кнопкой и решается броском. Здесь лежит то, из чего
 * складывается переговорная сцена: доводы, уступки и линия другой стороны.
 */

export const ARGUMENTS = ['might', 'kin', 'debt', 'fear', 'faith', 'gain', 'right'] as const
export type ArgumentId = (typeof ARGUMENTS)[number]

export const ARGUMENT_DEFS: Record<
  ArgumentId,
  { readonly label: string; readonly from: string; readonly says: string }
> = {
  might: {
    label: 'сила',
    from: 'счёт войск и мест (этап 161)',
    says: '«Посчитай мои копья и свои. Я не тороплю».',
  },
  kin: {
    label: 'родство',
    from: 'браки и общая родня',
    says: '«Твоя сестра носит моё имя. Об этом и говорим».',
  },
  debt: {
    label: 'долг',
    from: 'займы и дань',
    says: '«За тобой должок, и он записан не только у меня».',
  },
  fear: {
    label: 'страх',
    from: 'страх перед третьим (0.9)',
    says: '«Пока мы спорим, растёт не твоё и не моё».',
  },
  faith: {
    label: 'вера',
    from: 'церковь и помазание',
    says: '«Это скажу не я, а тот, кого ты слушаешь по воскресеньям».',
  },
  gain: {
    label: 'выгода',
    from: 'торг, дороги и пошлины',
    says: '«Считай не обиду, а серебро. Серебро говорит за меня».',
  },
  right: {
    label: 'право',
    from: 'грамоты, обычай и старые договоры',
    says: '«Есть бумага, и она старше нас обоих».',
  },
}

export const GIVES = ['silver', 'land', 'oath', 'kinTie', 'passage', 'nothing'] as const
export type GiveId = (typeof GIVES)[number]

export const GIVE_DEFS: Record<
  GiveId,
  { readonly label: string; readonly costs: string; readonly weight: number }
> = {
  silver: { label: 'серебро', costs: 'казна пустеет, и это видно по войску', weight: 2 },
  land: { label: 'земля', costs: 'место уходит навсегда, и его помнят твои люди', weight: 4 },
  oath: { label: 'слово', costs: 'связывает тебя, пока держишь', weight: 2 },
  kinTie: { label: 'родство', costs: 'дом входит в чужие споры', weight: 3 },
  passage: { label: 'проход', costs: 'чужое войско идёт твоей землёй', weight: 3 },
  nothing: { label: 'ничего', costs: 'ничего — и договора тоже', weight: 0 },
}

export const PARLEY = {
  /** Сколько ходов держится встреча, прежде чем другая сторона уходит. */
  moves: 5,
  /** Насколько довод весит, если он подкреплён миром. */
  backed: 2,
  /** Насколько — если не подкреплён. */
  hollow: 0.4,
  /** Сколько веса нужно, чтобы другая сторона согласилась. */
  agreesAt: 6,
  /** Ниже какого настроения она встаёт и уходит. */
  leavesAt: -4,
  /** Насколько пустой довод роняет настроение встречи. */
  annoys: -1.5,
} as const

export const PARLEY_WORDS = {
  opened: 'Сели говорить. У встречи есть повестка, и обе стороны её знают.',
  hollow: 'Довод пустой: за ним ничего не стоит, и это видно.',
  backed: 'Довод стоит на деле, и его принимают к счёту.',
  agreed: 'Сговорились: разговор кончается бумагой.',
  left: 'Он встал и вышел. Это тоже ответ.',
  nothing: 'Разошлись ни с чем: бумаги не будет.',
} as const
