/**
 * Тело (этап 186) — содержимое.
 *
 * Тело героя считалось по частям: усталость отдельно, рана отдельно, болезнь
 * отдельно, годы отдельно, — и нигде не сходилось в одно. Оттого «здоровье»
 * было полоской, а не состоянием человека. Здесь лежит то, из чего тело
 * складывается в одно число и в одни слова.
 */

export const FLESH = {
  /** Насколько рана отнимает от тела при тяжести 1. */
  woundTakes: 0.55,
  /** И болезнь. */
  ailmentTakes: 0.3,
  /** И усталость в сотню. */
  fatigueTakes: 0.35,
  /** И каждое увечье. */
  maimTakes: 0.08,
  /** С какого возраста тело начинает сдавать. */
  fadesFrom: 42,
  /** Сколько тело теряет за каждый год сверх. */
  fadesPerYear: 0.012,
  /** И сколько прибавляет молодость до зрелости. */
  youngAdds: 0.1,
  /** До какого возраста считается молодостью. */
  youngTo: 26,
  /** Ниже какой доли тело считается негодным для боя. */
  brokenAt: 0.45,
} as const

export const AGES = ['young', 'prime', 'grey', 'old'] as const
export type AgeId = (typeof AGES)[number]

export const AGE_DEFS: Record<
  AgeId,
  { readonly label: string; readonly opens: string; readonly shuts: string }
> = {
  young: {
    label: 'молодость',
    opens: 'Можно идти куда угодно и вставать после чего угодно.',
    shuts: 'Слушают тебя плохо: молодому не верят на слово.',
  },
  prime: {
    label: 'зрелость',
    opens: 'Тебя и слушают, и боятся: лучшие годы для всего сразу.',
    shuts: 'Ничего не закрыто — и потому ничего не оправдано.',
  },
  grey: {
    label: 'седина',
    opens: 'Слово весит больше удара: с тобой говорят как со старшим.',
    shuts: 'Долгий поход и долгий бой даются тяжело.',
  },
  old: {
    label: 'старость',
    opens: 'Тебя слушают молча: за тобой годы и память.',
    shuts: 'Воевать самому больше нельзя — можно посылать.',
  },
}

export const FLESH_WORDS = {
  whole: 'Тело здорово: ни раны, ни хвори, спина не болит.',
  tired: 'Устал так, что руки тяжелее оружия.',
  hurt: 'Рана свежая: всякое движение отдаётся.',
  sick: 'Болезнь держит: в глазах муть, ноги ватные.',
  maimed: 'Старое увечье напоминает о себе к перемене погоды.',
  broken: 'В таком теле не воюют: в седле не усидеть.',
} as const
