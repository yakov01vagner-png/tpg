/**
 * Молва (этап 101) — содержимое.
 *
 * Слух в 0.7 был оружием: пустил против короны — испортил ей отношения (этап
 * 82). Молвы как способа узнавать мир не было вовсе. Здесь она становится
 * третьим источником знания: самым дешёвым, самым быстрым и самым неверным.
 */

/** О чём вообще бывает молва. */
export const TALK_KINDS = ['host', 'stores', 'garrison', 'purse', 'aim'] as const
export type TalkKind = (typeof TALK_KINDS)[number]

export const TALK_WORDS: Record<TalkKind, { readonly says: string }> = {
  host: { says: 'Говорят, чужое войско видели' },
  stores: { says: 'Говорят, за теми стенами хлеба' },
  garrison: { says: 'Говорят, там под ружьём' },
  purse: { says: 'Говорят, в той казне' },
  aim: { says: 'Говорят, они задумали' },
}

export const GOSSIP = {
  /** Как часто молва делает шаг. */
  beat: 5,
  /** Сколько мест она захватывает за шаг. */
  reach: 3,
  /**
   * На сколько переходов заглядывает за новыми ушами.
   *
   * Три, а не один: между поселениями в этом мире лежат броды, перевалы и
   * курганы (этап 32), и ближайший сосед с людьми — обычно в трёх переходах.
   */
  hops: 3,
  /** Сколько суток живёт, если её не подогревать. */
  livesDays: 90,
  /** На сколько искажается число за каждый переход, долей. */
  twistPerHop: 0.18,
  /** Какая доля молвы вообще правдива в зерне. */
  trueShare: 0.45,
  /** Сколько стоит пустить свою молву. */
  startSilver: 220,
  /** Сколько суток уходит на проверку услышанного. */
  checkDays: 3,
  /** И сколько это стоит. */
  checkSilver: 140,
  /** Насколько уличённая ложь роняет доверие к молве вообще. */
  exposedTrust: -0.1,
} as const

export const GOSSIP_WORDS = {
  born: 'На торгу заговорили.',
  spread: 'Молва пошла дальше.',
  died: 'О том уже не говорят.',
  checked: 'Проверено: молва не соврала.',
  exposed: 'Проверено: молва врёт.',
  mine: 'Молва пущена тобой — дальше она живёт сама.',
} as const
