/**
 * Он делает выводы (этап 119) — содержимое.
 *
 * Картина мира этапа 118 сказала, что корона знает. Здесь она начинает из этого
 * знания выводить: не «сколько у него людей», а «чего он хочет». Вывод бывает
 * верным и неверным, называется вслух и меняет поведение заранее — вот чего в
 * 0.7 не было вовсе: там ИИ отвечал на удар, а не на замысел.
 */

/** Чего, по мнению короны, добивается игрок. */
export const PLAYER_AIMS = ['takeLand', 'tribute', 'trade', 'marry', 'hold', 'none'] as const
export type PlayerAim = (typeof PLAYER_AIMS)[number]

export const PLAYER_AIM_DEFS: Record<
  PlayerAim,
  { readonly label: string; readonly about: string; readonly prepares: string }
> = {
  takeLand: {
    label: 'он идёт за землёй',
    about: 'Собирает людей у границы и не ищет ни родства, ни торга.',
    prepares: 'Гарнизоны по границе усиливают, хлеб свозят за стены.',
  },
  tribute: {
    label: 'он придёт за данью',
    about: 'Ему нужно серебро, а не земля: такой торгуется, а не воюет до конца.',
    prepares: 'Казну прячут, послов держат наготове.',
  },
  trade: {
    label: 'он торгует',
    about: 'Его сила в деньгах, а не в копьях. Пока — не опасен.',
    prepares: 'Пошлины поднимают, дороги смотрят.',
  },
  marry: {
    label: 'он ищет родства',
    about: 'Ходит послами и сватается: хочет войти в семью, а не взять силой.',
    prepares: 'Дочерей считают, цену родства поднимают.',
  },
  hold: {
    label: 'он держит своё',
    about: 'Сидит на своём и никуда не тянется.',
    prepares: 'За ним просто присматривают.',
  },
  none: {
    label: 'непонятно, чего он хочет',
    about: 'Его ходы не складываются в замысел. Или складываются, но не в этот.',
    prepares: 'Ничего особенного не делают: неясное не тревожит.',
  },
}

/** По каким приметам он судит. */
export const TELLS = ['muster', 'border', 'envoys', 'purse', 'sieges', 'noise'] as const
export type TellKind = (typeof TELLS)[number]

export const TELL_DEFS: Record<
  TellKind,
  { readonly label: string; readonly about: string; readonly weight: number }
> = {
  muster: {
    label: 'сбор войска',
    about: 'У него больше людей, чем нужно для порядка.',
    weight: 0.3,
  },
  border: {
    label: 'войско у границы',
    about: 'Его части стоят там, откуда ходят не домой.',
    weight: 0.4,
  },
  envoys: { label: 'послы', about: 'Его люди ездят чаще, чем обычно.', weight: 0.25 },
  purse: { label: 'серебро', about: 'Он богат, и это видно по его закупкам.', weight: 0.2 },
  sieges: { label: 'осады', about: 'Он уже берёт города. Дальше будет брать ещё.', weight: 0.5 },
  noise: {
    label: 'пустые ходы',
    about: 'Он делает то, что ничего не значит. Или значит не то.',
    weight: -0.3,
  },
}

export const GUESS = {
  /** Раз во сколько суток корона пересматривает вывод. */
  beat: 15,
  /** С какой уверенности вывод считается принятым. */
  acts: 0.45,
  /** Насколько повторение примет прибавляет уверенности за раз. */
  learnsPerSeen: 0.06,
  /** Дальше этого уверенность не растёт. */
  sure: 0.9,
  /** Насколько каждый пустой ход сбивает вывод. */
  noiseCuts: 0.12,
  /** Насколько крепче гарнизон у того, кто приготовился. */
  readyGarrison: 1.35,
} as const

export const GUESS_WORDS = {
  reads: 'Из того, что ты делаешь, он выводит, чего ты хочешь.',
  named: 'Он называет твой замысел вслух.',
  wrong: 'И называет неверно.',
  ready: 'Он готовится заранее, а не после удара.',
  confused: 'Твои пустые ходы сбили его: он не понимает, к чему ты ведёшь.',
  learns: 'Повторяющееся он замечает быстрее случайного.',
} as const
