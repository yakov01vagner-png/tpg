import type { LordTemper } from './castle'

/**
 * Присяга и вассалы (этап 74) — содержимое.
 *
 * В 0.5 вассал был числом верности и долей подати: лорд уходил под твою руку,
 * если тебе верил, и отдавал треть. Присяги как договора не было — а она и есть
 * то, чем держава отличается от владения: у неё две стороны, и каждая чего-то
 * хочет.
 *
 * Здесь лежит то, чего в присяге авторского: чем вассал платит, чего просит за
 * это, какими словами соглашается и отказывается, и что двигает его верность.
 */

/** Чем вассал платит сюзерену. */
export const OATH_GIVES = ['service', 'tax', 'both'] as const
export type OathGives = (typeof OATH_GIVES)[number]

export const OATH_GIVES_DEFS: Record<
  OathGives,
  { readonly label: string; readonly about: string }
> = {
  service: {
    label: 'служба',
    about: 'Приводит людей на твою войну, но подати с земли не платит: земля кормит его копья.',
  },
  tax: {
    label: 'подать',
    about: 'Платит долю с земли и на войну не идёт: с него берут серебром, а не кровью.',
  },
  both: {
    label: 'служба и подать',
    about: 'И людей, и долю. Такую присягу дают редко и держат недолго.',
  },
}

/**
 * Чего вассал просит за присягу и насколько он разборчив.
 *
 * `demand` — порог: сколько нужно набрать того, что вассал видит (твоя слава,
 * его милость к тебе, цена земли), чтобы он согласился. Гордец не служит
 * безымянному, а расчётливый пойдёт и к безымянному, если земля того стоит.
 */
export const OATH_TEMPERS: Record<
  LordTemper,
  {
    /** Что он даёт, если согласится. */
    readonly gives: OathGives
    /** Какую долю подати отдаёт. */
    readonly share: number
    /** Просит ли оставить ему свой суд. */
    readonly justice: boolean
    /** Сколько людей обязуется приводить с каждой сотни своих. */
    readonly levy: number
    /** Насколько он разборчив. */
    readonly demand: number
    /** Чего просит словами. */
    readonly asks: string
    readonly agrees: string
    readonly refuses: string
  }
> = {
  proud: {
    gives: 'service',
    share: 0,
    justice: true,
    levy: 0.5,
    demand: 0.75,
    asks: 'Суд на моей земле — мой. Подати с меня не спрашивай: я плачу копьями.',
    agrees: 'Моя рука твоя, пока ты помнишь, кто я.',
    refuses: 'Служить тебе? Назови сперва, чем ты знаменит.',
  },
  shrewd: {
    gives: 'both',
    share: 0.2,
    justice: true,
    levy: 0.3,
    demand: 0.5,
    asks: 'Пятую часть подати и суд у себя. Остальное — по обычаю.',
    agrees: 'По рукам. Условия помню я, а не бумага.',
    refuses: 'Считал и не сошлось. Приходи, когда предложишь больше.',
  },
  jovial: {
    gives: 'tax',
    share: 0.3,
    justice: false,
    levy: 0.15,
    demand: 0.35,
    asks: 'Треть подати бери, суд оставь себе, а на войну зови только всерьёз.',
    agrees: 'Отчего нет? С тобой и посидеть найдётся о чём.',
    refuses: 'Да ну её, эту присягу. Мне и так неплохо.',
  },
  grim: {
    gives: 'service',
    share: 0.1,
    justice: true,
    levy: 0.6,
    demand: 0.6,
    asks: 'Я привожу людей. Много. Взамен — не лезь в мою землю.',
    agrees: 'Сказано. Слово своё я держу крепче, чем ты своё.',
    refuses: 'Нет.',
  },
  pious: {
    gives: 'both',
    share: 0.3,
    justice: false,
    levy: 0.3,
    demand: 0.45,
    asks: 'По обычаю: треть подати и люди на войну. Клятву дают перед храмом.',
    agrees: 'Перед лицом неба: служу.',
    refuses: 'Не всякой руке присягают. Твоей — не могу.',
  },
  greedy: {
    gives: 'tax',
    share: 0.15,
    justice: true,
    levy: 0.1,
    demand: 0.4,
    asks: 'Седьмую часть — и ни человека без отдельной платы.',
    agrees: 'Твоя земля, моя выгода. Согласен.',
    refuses: 'Мало даёшь. За такую землю и присяги не жалко, да не такой.',
  },
}

/** Ниже этой верности вассал на зов не идёт. */
export const OATH_BREAKS = 25

/**
 * Что двигает верность вассала за сутки.
 *
 * Не бросок и не «отношение»: понятные вещи, которые вассал видит у себя во
 * дворе. Числа малые нарочно — верность должна набираться годами, а терять её
 * должно быть можно за один суд.
 */
export const LOYALTY_DRIFT = {
  /** Тяжёлая подать в державе. */
  heavyTax: -0.03,
  /** Лёгкая подать. */
  lightTax: 0.02,
  /** Свой суд оставлен ему. */
  ownJustice: 0.015,
  /** Суд отобран. */
  noJustice: -0.01,
  /** Ты в войне, а его не звали: люди сидят дома, пока сосед воюет. */
  warIdle: -0.01,
  /** Мир и тишина. */
  peace: 0.01,
  /** За каждый позор в твоей славе (этап 68). */
  shame: -0.02,
  /** За соседа-соперника среди твоих же вассалов. */
  rival: -0.008,
} as const

/** Сколько дней подряд вассал помнит, что его звали. */
export const CALLED_DAYS = 60

/** Титул по числу держаний: земля делает титул, а не наоборот. */
export const VASSAL_TITLES: readonly { readonly fiefs: number; readonly title: string }[] = [
  { fiefs: 1, title: 'барон' },
  { fiefs: 3, title: 'граф' },
  { fiefs: 6, title: 'герцог' },
]
