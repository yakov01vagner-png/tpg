/**
 * Долгие процессы (этап 145) — содержимое.
 *
 * У короны была нынешняя сила и не было истории: вчера и позавчера она
 * выглядела так же, как сегодня. Оттого упадок нельзя было увидеть, пока он не
 * кончился. Здесь у державы появляется кривая длиной в десятилетия.
 */

/** Чем объясняется упадок (Дл2). */
export const REASONS = ['wars', 'famine', 'split', 'trade'] as const
export type ReasonId = (typeof REASONS)[number]

export const REASON_DEFS: Record<ReasonId, { readonly label: string; readonly says: string }> = {
  wars: { label: 'войны', says: 'Войны без конца: земля убывает быстрее, чем родятся люди.' },
  famine: { label: 'недород', says: 'Недород: людей стало меньше, а хлеба ещё меньше.' },
  split: { label: 'раздел', says: 'Раздел наследства: держава разошлась по рукам наследников.' },
  trade: { label: 'потеря торга', says: 'Торг ушёл в другие руки: города беднеют первыми.' },
}

export const CURVE = {
  /** Раз во сколько суток берётся замер кривой. */
  beat: 360,
  /** Сколько замеров помнится: двадцать лет. */
  keep: 20,
  /** Насколько должна измениться земля, чтобы это считалось процессом, — доля. */
  moves: 0.12,
  /** Сколько замеров подряд должно идти в одну сторону. */
  runs: 3,
} as const

export const CURVE_WORDS = {
  rise: 'Возвышение: держава растёт не год и не два.',
  fall: 'Упадок: держава убывает десятилетиями, и это видно заранее.',
  still: 'Держава стоит: ни вверх, ни вниз.',
  land: 'Земля идёт своей кривой: провинции пустеют и наполняются десятилетиями.',
} as const
