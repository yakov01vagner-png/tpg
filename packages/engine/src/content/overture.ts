/**
 * ИИ в дипломатии (этап 91) — содержимое.
 *
 * Посольства до 0.7 ездили только в одну сторону: игрок слал, короны отвечали.
 * Здесь короны начинают говорить первыми — предлагать, торговаться, обещать и
 * не исполнять, — и держат счёт тому, кто держит слово.
 */

/** С чем приезжает чужой посол (Ди1). */
export const OVERTURES = ['alliance', 'marriage', 'tribute', 'passage', 'threat', 'join'] as const
export type OvertureKind = (typeof OVERTURES)[number]

export const OVERTURE_DEFS: Record<
  OvertureKind,
  {
    readonly label: string
    readonly about: string
    /** Что это даёт тому, кто соглашается. */
    readonly gives: string
    /** Чего это стоит. */
    readonly costs: string
    /** Насколько такое предложение тяжело принять. */
    readonly weight: number
  }
> = {
  alliance: {
    label: 'союз',
    about: 'Общий враг и общая война: вместе — или порознь, но тогда без нас.',
    gives: 'войско союзника и спокойная граница',
    costs: 'чужая война становится твоей',
    weight: 2,
  },
  marriage: {
    label: 'брак',
    about: 'Родство вместо войска: дешевле, дольше и без крови.',
    gives: 'родство и мир на поколение',
    costs: 'чужая кровь в своём доме и чужие права на твою землю',
    weight: 1,
  },
  tribute: {
    label: 'дань',
    about: 'Плати — и войны не будет. Пока платишь.',
    gives: 'мир, купленный серебром',
    costs: 'серебро каждый день и позор при всех',
    weight: 3,
  },
  passage: {
    label: 'проход войску',
    about: 'Пропусти — и мы не тронем твоего.',
    gives: 'целая земля и чужая благодарность',
    costs: 'чужое войско у себя за спиной',
    weight: 1.5,
  },
  threat: {
    label: 'угроза',
    about: 'Отдай, пока просят: потом придут и возьмут.',
    gives: 'отсрочка',
    costs: 'то, что требуют',
    weight: 3.5,
  },
  join: {
    label: 'звать в войну',
    about: 'Мы уже воюем. Встань рядом — и добыча пополам.',
    gives: 'доля в чужой победе',
    costs: 'новый враг',
    weight: 2.5,
  },
}

/** Чем кончается разговор (Ди2). */
export const ANSWERS = ['accept', 'refuse', 'counter'] as const
export type AnswerId = (typeof ANSWERS)[number]

export const OVERTURE = {
  /** Сколько суток держится предложение, прежде чем посол уедет. */
  standDays: 45,
  /** Через сколько суток корона возвращается с тем же разговором. */
  returnDays: 120,
  /** Сколько предложений одна корона делает за год. */
  perYear: 2,
  /** Насколько встречное условие должно быть легче, чтобы его приняли. */
  counterEase: 0.35,
  /** Во сколько раз сильнее должен быть грозящий, чтобы за угрозой стояли силы. */
  threatBacking: 1.2,
  /** Насколько падает слово короны за одно нарушенное обещание. */
  wordBite: 25,
  /** Насколько слово отрастает за год без обмана. */
  wordHeal: 6,
  /** Во сколько раз больше просит тот, кого держат обманщиком. */
  liarPrice: 1.4,
  /** Во сколько раз сильнее должен вырасти один, чтобы против него сошлись. */
  giantLine: 1.6,
} as const

export const OVERTURE_WORDS = {
  none: 'Послов нет: о тебе сейчас не говорят.',
  waiting: 'Посол ждёт ответа.',
  left: 'Посол уехал, не дождавшись.',
  hollow: 'За этой угрозой нет сил: их войско не дойдёт и до границы.',
  backed: 'Угроза не пустая: их войско сильнее твоего.',
  liar: 'Эта корона уже нарушала слово: с ней говорят иначе.',
  trusted: 'Эта корона слово держит: это знают все.',
} as const
