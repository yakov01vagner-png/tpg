/**
 * Он обманывает нарочно (этап 121) — содержимое.
 *
 * До сих пор корона могла ошибаться, но не могла лгать: её слово всегда
 * означало её намерение. Здесь у неё появляется то же, что у игрока с этапа
 * 112, — выгода от чужого незнания. Обещание, которого она не сдержит; сила,
 * которой нет; слабость, которой нет. И то же наказание: пойманный на обмане
 * теряет слово надолго.
 */

/** Чем именно она обманывает. */
export const DECEITS = ['promise', 'weak', 'strong'] as const
export type DeceitKind = (typeof DECEITS)[number]

export const DECEIT_DEFS: Record<
  DeceitKind,
  {
    readonly label: string
    readonly about: string
    /** Во сколько раз она показывает свою силу против правды. */
    readonly shows: number
    /** Чего она этим добивается. */
    readonly wants: string
  }
> = {
  promise: {
    label: 'обещание, которого не сдержит',
    about: 'Она даёт слово, зная, что не исполнит: ей нужно, чтобы ты пока не мешал.',
    shows: 1,
    wants: 'выиграть время',
  },
  weak: {
    label: 'ложная слабость',
    about: 'Показывает меньше, чем имеет: пусть ударят не там и не теми силами.',
    shows: 0.55,
    wants: 'ударили не там',
  },
  strong: {
    label: 'ложная сила',
    about: 'Показывает больше, чем имеет: пусть не ударят вовсе.',
    shows: 1.6,
    wants: 'не ударили вовсе',
  },
}

export const DECEIT = {
  /** Раз во сколько суток корона решает, обманывать ли. */
  beat: 18,
  /** Насколько слабый чаще прикидывается сильным. */
  weakBluffs: 0.5,
  /** И насколько сильный чаще прикидывается слабым. */
  strongHides: 0.3,
  /** Насколько обман виден тому, у кого там свои люди. */
  seenByEyes: 0.45,
  /** И насколько — тому, у кого вести расходятся. */
  seenByClash: 0.35,
  /** На сколько суток пойманный теряет слово. */
  wordLostDays: 720,
  /** Насколько дешевеет его слово, пока он в этом уличён. */
  wordWorth: 0.35,
} as const

export const DECEIT_WORDS = {
  lied: 'Он сказал не то, что есть, и знал это.',
  promised: 'Он дал слово, которого не сдержит.',
  weak: 'Он прикидывается слабее, чем есть.',
  strong: 'Он показывает силу, которой у него нет.',
  seen: 'Обман раскрыт заранее.',
  clash: 'Вести о нём расходятся — значит, кто-то врёт.',
  punished: 'Пойманный на обмане теряет слово надолго: теперь ему не верят и там, где он честен.',
  held: 'Обман держится: ты веришь тому, что тебе показали.',
} as const
