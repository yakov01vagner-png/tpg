/**
 * Обман на войне (этап 112) — содержимое.
 *
 * Туман этапов 109–111 сделал незнание вещью. Обман — это то, ради чего
 * незнание вообще стоит иметь: пока все всё видели, врать было некому. Здесь
 * ложь становится ходом со своей ценой, своим сроком и своим разоблачением.
 */

export const RUSES = ['camp', 'demo', 'rumour', 'ambush'] as const
export type RuseKind = (typeof RUSES)[number]

export const RUSE_DEFS: Record<
  RuseKind,
  {
    readonly label: string
    readonly about: string
    /** Сколько стоит завести. */
    readonly cost: number
    /** Сколько суток держится сам по себе. */
    readonly days: number
    /** Сколько людей на это нужно отвлечь. */
    readonly men: number
    /** Насколько трудно раскусить: 0 — видно сразу, 1 — почти никогда. */
    readonly holds: number
  }
> = {
  camp: {
    label: 'ложный лагерь',
    about: 'Костры, коновязи, вытоптанная трава. Войска нет, а след есть.',
    cost: 300,
    days: 14,
    men: 20,
    holds: 0.6,
  },
  demo: {
    label: 'демонстрация',
    about: 'Показное движение к месту, которое брать не собираешься.',
    cost: 150,
    days: 8,
    men: 60,
    holds: 0.5,
  },
  rumour: {
    label: 'пущенный слух',
    about: 'Весть о своей силе или слабости, пущенная на торгу (этап 101).',
    cost: 220,
    days: 30,
    men: 0,
    holds: 0.45,
  },
  ambush: {
    label: 'засада',
    about: 'Часть, скрытая на чужом пути. Тот, кто в неё войдёт, узнает об этом последним.',
    cost: 0,
    days: 20,
    men: 0,
    holds: 0.75,
  },
}

export const RUSE = {
  /** Насколько дозор чужой стороны помогает раскусить обман. */
  scoutSees: 0.45,
  /** И насколько — ум того, кто смотрит, за очко сверх шести. */
  witsPerPoint: 0.06,
  /** Сколько людей демонстрация оттягивает на себя, долей от показанного. */
  pulls: 0.8,
  /** На сколько переходов демонстрация вообще действует. */
  pullHops: 4,
  /** Сколько духа теряет тот, кто вошёл в засаду. */
  ambushMorale: 22,
  /** Раз во сколько суток считаются чужие обманы. */
  beat: 4,
} as const

export const RUSE_WORDS = {
  made: 'Обман заведён: теперь он живёт сам.',
  held: 'Держится: они верят.',
  seen: 'Раскусили: тебя больше не слушают в этом.',
  pulled: 'Они повернули на показанное.',
  sprung: 'Засада сработала: их встретили там, где не ждали.',
  theirs: 'Кажется, тебя водят за нос: то, что ты видишь, слишком удобно.',
  gone: 'Обман кончился сам: костры остыли.',
} as const
