/**
 * Постоянный посол (этап 117) — содержимое.
 *
 * Посольство этапа 114 — это выезд: съездил, посмотрел, вернулся. Постоянный
 * посол — другое: он живёт там. Его вести свежее всех прочих, и оттого он
 * дороже всех прочих; а живя там годами, он начинает смотреть их глазами, и
 * это единственная беда, которую нельзя купить.
 */

export const RESIDENT = {
  /** Сколько стоит посадить своего человека при чужом дворе. */
  setUp: 1200,
  /** И сколько он стоит в сутки: дом, стол, подарки. */
  perDay: 14,
  /** Раз во сколько суток он присылает вести. */
  beat: 10,
  /** Насколько уже вилка его вестей против обычного посольства. */
  narrows: 0.5,
  /** Через сколько суток он начинает прирастать к месту. */
  nativeAfter: 540,
  /** И насколько сильно прирастает за каждый следующий год. */
  nativePerYear: 0.22,
  /** Дальше этого он их человек больше, чем твой. */
  nativeMax: 0.8,
  /** Раз во сколько суток считается риск. */
  riskBeat: 20,
  /** Насколько велик риск при худом отношении. */
  expelAt: -35,
  /** Сколько платят за то, чтобы он стал их человеком. */
  buyPrice: 4000,
} as const

export const RESIDENT_RISKS = ['none', 'expelled', 'bought', 'caught'] as const
export type ResidentRisk = (typeof RESIDENT_RISKS)[number]

export const RISK_DEFS: Record<ResidentRisk, { readonly label: string; readonly about: string }> = {
  none: { label: 'всё тихо', about: 'Он сидит, слушает и пишет.' },
  expelled: {
    label: 'выслан',
    about: 'Ему велели выехать в трое суток. Причина не объясняется.',
  },
  bought: {
    label: 'купили',
    about: 'Он всё ещё пишет тебе — и пишет то, что им угодно.',
  },
  caught: {
    label: 'уличён',
    about: 'Его взяли с чужими письмами. Это уже не посольство, а дело.',
  },
}

export const RESIDENT_WORDS = {
  seated: 'Твой человек сел при их дворе.',
  writes: 'Пишет с их двора.',
  native: 'Он живёт там слишком давно: в его письмах всё больше их правоты.',
  gone: 'Посольства там больше нет.',
  theirs: 'И у тебя сидят чужие: что они видят, решаешь ты.',
  costly: 'Постоянное посольство стоит дорого и каждый день.',
} as const
