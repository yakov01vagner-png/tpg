/**
 * ИИ и равновесие (этап 143) — содержимое.
 *
 * Короны умели считать силу и замысел; здесь они начинают считать равновесие —
 * то же, что считает мир вокруг игрока (этап 135). Союз держится не только
 * отношением, но и тем, что союзник не вырвался вперёд.
 */

export const BALANCE = {
  /** Раз во сколько суток короны пересчитывают равновесие между собой. */
  beat: 30,
  /** С какой доли пути союзника бросают. */
  betrayAt: 0.8,
  /** Насколько продвижение цели поднимает вероятность объявления войны. */
  perShare: 0.85,
  /** Ниже этого давление не опускается: кубик остаётся на мелочи. */
  least: 0.15,
  /** Насколько связанность с целью гасит давление: ниже всякого дна. */
  bound: 0.05,
} as const

export const BALANCE_WORDS = {
  counted:
    'Равновесие считается, а не чувствуется: короны складываются против первого, кто бы им ни был.',
  betrayed: 'Союз рвётся по расчёту: вырвавшегося вперёд бросают, и это не ссора.',
  dice: 'Война объявляется расчётом: кубик остаётся на мелочи, а причина — на равновесии.',
  wrong: 'Ошибиться они могут и здесь: равновесие считается по вестям, а вести врут.',
} as const
