/**
 * Спутники, у которых своя жизнь (этап 167) — содержимое.
 *
 * У спутника с 0.2 есть нрав, дело и расположение к тебе. Не хватало главного:
 * он ничего не хотел от тебя вслух. Своё дело двигалось само, доля не просилась,
 * обещания не давались и не нарушались, и оттого человек рядом всё равно
 * оставался мешком с умениями. Здесь лежит то, о чём он говорит.
 */

export const KITH_ASKS = ['share', 'place', 'help', 'leave'] as const
export type KithAsk = (typeof KITH_ASKS)[number]

export const KITH_ASK_DEFS: Record<
  KithAsk,
  {
    readonly label: string
    readonly says: string
    /** Во что обходится «да». Ноль — не деньгами. */
    readonly cost: number
    /** Насколько поднимется расположение, если сделать. */
    readonly grants: number
    /** И насколько упадёт, если отказать. */
    readonly refused: number
    /** Сколько суток он ждёт обещанного. */
    readonly waits: number
  }
> = {
  share: {
    label: 'доля',
    says: 'Мы берём города, а я хожу в том же платье. Мне доля, а не жалованье.',
    cost: 900,
    grants: 12,
    refused: -10,
    waits: 60,
  },
  place: {
    label: 'место',
    says: 'Я с тобой не первый год. Дай мне место — не из милости, а по службе.',
    cost: 0,
    grants: 18,
    refused: -14,
    waits: 180,
  },
  help: {
    label: 'помощь его делу',
    says: 'Ты знаешь, зачем я здесь. Помоги мне с этим — и дальше я твой.',
    cost: 0,
    grants: 14,
    refused: -8,
    waits: 365,
  },
  leave: {
    label: 'отпустить',
    says: 'Отпусти. Моё дело стоит, пока я хожу за тобой.',
    cost: 0,
    grants: 6,
    refused: -16,
    waits: 30,
  },
} as const

export const KITH = {
  /** Раз во сколько суток спутники говорят и считают. */
  beat: 30,
  /** Со скольких лет службы он начинает просить место. */
  placeYears: 3,
  /** Ниже какого расположения он просится уйти. */
  leaveMood: 30,
  /** С какого возраста он думает о покое. */
  oldAt: 52,
  /** И насколько охотно уходит на покой за каждый год сверх. */
  oldLeaves: 0.08,
  /** Насколько падает расположение за нарушенное обещание. */
  broken: -18,
  /** И растёт за сдержанное. */
  kept: 10,
  /** Насколько ссоры в отряде тянут расположение за такт. */
  quarrel: -1,
  /** Сколько обещаний помнится. */
  remembers: 40,
} as const

export const KITH_WORDS = {
  own: 'У него своё, и оно не про тебя.',
  waiting: 'Он ждёт обещанного и считает дни.',
  brokenWord: 'Ты обещал и не сделал. Это помнят дольше всего прочего.',
  old: 'Годы берут своё: ему хочется дожить, а не дойти.',
  done: 'Его дело сделано — и дальше он идёт с тобой уже по своей воле.',
} as const
