/**
 * Закон, который применяют (этап 181) — содержимое.
 *
 * Закон с 0.7 — четыре ползунка: подать, пошлина, ополчение, суд. Он ставится
 * раз и действует везде одинаково: нет ни тех, кто его не исполняет, ни тех,
 * кому он поперёк обычая, ни цены за перемену. Здесь лежит то, чем закон
 * встречают.
 */

export const WRIT = {
  /** Насколько вассал с низкой верностью недодаёт по указу. */
  sulks: 0.5,
  /** Ниже какой верности он начинает саботировать. */
  sulksBelow: 45,
  /** Насколько город с грамотой исполняет указ хуже прочих. */
  chartered: 0.6,
  /** Насколько место, где порядок — сход, сопротивляется суду по-твоему. */
  elders: 0.75,
  /** И цех — подати. */
  guild: 0.8,
  /** Сколько суток идёт слух об указе до дальних мест. */
  spreads: 20,
  /** Во что обходится перемена закона: доля памяти мест за каждую ступень. */
  changeCosts: 4,
  /** Сколько суток закон считается новым: пока он нов, его проверяют на прочность. */
  fresh: 60,
} as const

export const WRIT_WORDS = {
  obeyed: 'Указ исполняют: недоимок нет, и говорить не о чем.',
  sulked: 'Указ исполняют вполсилы: кто может не дать, тот не даёт.',
  custom: 'Здесь свой порядок, и он старше твоего указа.',
  fresh: 'Закон нов: его пробуют на прочность.',
  changed: 'Перемену закона помнят дольше, чем сам закон.',
  both: 'У всякого правила две стороны, и обе названы.',
} as const
