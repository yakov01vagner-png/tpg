/**
 * Осадное дело (этап 85) — содержимое.
 *
 * До 0.7 крепость была одним числом: есть стены — обороне вдвое легче, нет —
 * почти ничего. Осада у ИИ была отсчётом: двенадцать суток под стенами, потом
 * приступ, чем бы дело ни пахло. Здесь у крепости появляются свои числа — вода,
 * запас, башни, донжон, — а у осады появляется выбор: строить пороки, морить
 * голодом или договариваться.
 */

/** Откуда в крепости вода (О1). */
export const WATERS = ['well', 'cistern', 'river', 'none'] as const
export type WaterId = (typeof WATERS)[number]

export const WATER_DEFS: Record<
  WaterId,
  { readonly label: string; readonly about: string; readonly days: number }
> = {
  well: {
    label: 'колодец',
    about: 'Вода внутри стен: осаждать такую крепость придётся хлебом, а не жаждой.',
    // Колодец не перекрыть: осада упирается в запас хлеба, а не в воду.
    days: 9999,
  },
  cistern: {
    label: 'цистерна',
    about: 'Дождевой запас под башней: хватает надолго, но не навсегда.',
    days: 70,
  },
  river: {
    label: 'река под стеной',
    about: 'Воду берут с реки — пока её не перекрыли. Перекрывают быстро.',
    days: 24,
  },
  none: {
    label: 'привозная вода',
    about: 'Воду возят из ручья за стеной. Осада кончает это в первую же неделю.',
    days: 9,
  },
}

/** Осадные машины (О2). */
export const ENGINES = ['ram', 'tower', 'trebuchet'] as const
export type EngineId = (typeof ENGINES)[number]

export const ENGINE_DEFS: Record<
  EngineId,
  {
    readonly label: string
    readonly about: string
    /** Сколько суток строит десяток человек. */
    readonly days: number
    /** Серебро на железо и канаты. */
    readonly silver: number
    /** Сколько рук нужно, чтобы вообще начать. */
    readonly men: number
    /** Какую долю помощи стен снимает. */
    readonly relief: number
    /** Насколько реже башни достают идущих на приступ. */
    readonly cover: number
  }
> = {
  ram: {
    label: 'таран',
    about: 'Бревно в оковке под навесом: ворота не стена, их можно выбить.',
    days: 4,
    silver: 260,
    men: 12,
    relief: 0.35,
    cover: 0,
  },
  tower: {
    label: 'осадная башня',
    about: 'Помост вровень со стеной: люди идут по настилу, а не по лестнице.',
    days: 9,
    silver: 700,
    men: 20,
    relief: 0.3,
    cover: 0.45,
  },
  trebuchet: {
    label: 'порок',
    about: 'Камнемёт: бьёт по башням и кладке, пока стрелки не перестанут мешать.',
    days: 12,
    silver: 1100,
    men: 16,
    relief: 0.25,
    cover: 0.35,
  },
}

/** На каких условиях сдаются (О3). */
export const TERMS = ['free', 'ransom', 'mercy'] as const
export type TermId = (typeof TERMS)[number]

export const TERM_DEFS: Record<
  TermId,
  {
    readonly label: string
    readonly about: string
    /** Насколько такие условия легче принять. */
    readonly ease: number
  }
> = {
  free: {
    label: 'свободный выход',
    about: 'Гарнизон уходит с оружием и знамёнами. Место целое, люди живы, враг тоже.',
    ease: 0.35,
  },
  ransom: {
    label: 'выкуп',
    about:
      'Город откупается серебром и остаётся за прежним хозяином. Осада кончается ничем, кроме денег.',
    ease: 0.15,
  },
  mercy: {
    label: 'сдача на милость',
    about: 'Ворота открывают и кладут оружие. Что будет дальше — воля того, кто под стенами.',
    ease: -0.2,
  },
}

export const SIEGE = {
  /** Сколько хлеба съедает человек гарнизона в сутки. */
  perMan: 0.05,
  /** Сколько народу кормится за стенами на каждого гарнизонного. */
  mouths: 3.5,
  /** Сколько духа отнимает у гарнизона каждые сутки жажды. */
  thirstBite: 0.09,
  /** Во сколько раз башни добавляют потерь идущим на приступ. */
  towerBite: 0.018,
  /** Доля потерь на голых стенах без машин. */
  stormLoss: 0.07,
  /** Насколько донжон продлевает дело: взяли стены — крепость ещё не взята. */
  keepDays: 14,
  /** Сколько держится открытое место, у которого и ворот-то нет. */
  openDays: 3,
  /** За сколько переходов гарнизон ждёт выручки. */
  reliefReach: 3,
  /** Сколько стоят сутки припаса в крепость, за меру хлеба. */
  storeSilver: 14,
} as const

export const SIEGE_WORDS = {
  holds: 'Крепость держится: есть и вода, и хлеб.',
  thirst: 'В крепости считают воду.',
  hunger: 'В крепости считают хлеб.',
  doomed: 'Крепость не выстоит: ни воды, ни хлеба.',
  relief: 'Гарнизон ждёт выручки: свои недалеко.',
  alone: 'Выручки ждать неоткуда.',
} as const
