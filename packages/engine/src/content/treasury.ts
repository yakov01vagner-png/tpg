/**
 * Казна и войско короны (этап 77) — содержимое.
 *
 * Деньги в игре были всегда, но были кошельком: одно число, которое растёт от
 * подати и убывает от жалованья. Держава требует другого — счёта: откуда пришло,
 * куда ушло, чем можно занять и что бывает, когда не хватает.
 *
 * Здесь лежит то, что у этого авторского: заимодавцы, их условия и слова, три
 * способа поставить людей под ружьё и то, чем отзывается пустая казна.
 */

/** У кого занимают. */
export const LENDERS = ['merchants', 'guild', 'temple'] as const
export type LenderId = (typeof LENDERS)[number]

export const LENDER_DEFS: Record<
  LenderId,
  {
    readonly label: string
    readonly about: string
    /** Рост долга за сутки. */
    readonly rate: number
    /** Сколько дают: доля от годового прихода державы. */
    readonly share: number
    /** Что говорят, когда дают. */
    readonly gives: string
    /** И когда не дают. */
    readonly refuses: string
    /** Чем грозят, когда не платишь. */
    readonly angry: string
  }
> = {
  merchants: {
    label: 'купеческий дом',
    about: 'Дают быстро, берут дорого и считают каждый день.',
    rate: 0.0009,
    share: 0.5,
    gives: 'Серебро сегодня, счёт завтра. Мы считаем по дням.',
    refuses: 'Под такое имя мы не даём. Приходи, когда за тобой будет земля.',
    angry: 'Долг растёт. На рынках о тебе уже говорят, и говорят нехорошо.',
  },
  guild: {
    label: 'гильдия',
    about: 'Дают много и надолго, но помнят обиды всем цехом.',
    rate: 0.0006,
    share: 0.8,
    gives: 'Гильдия даёт. Гильдия и спросит — со всеми своими.',
    refuses: 'Гильдия тебя не знает настолько, чтобы давать столько.',
    angry: 'Гильдия недовольна: её люди в твоих городах это чувствуют.',
  },
  temple: {
    label: 'храм',
    about: 'Берут мало, дают немного и помнят не деньгами.',
    rate: 0.0002,
    share: 0.3,
    gives: 'Возьми. Отдашь, когда сможешь, — но отдашь.',
    refuses: 'Храм не даёт тем, кто не бывает в храме.',
    angry: 'Храм молчит о твоём долге вслух — и это хуже, чем если бы говорил.',
  },
}

/** Сколько дней подряд можно не платить по долгу, прежде чем заимодавец обидится. */
export const DEBT_PATIENCE = 30

/** Ниже этого имени в глазах заимодавца не дают вовсе. */
export const LEND_STANDING = -20

/**
 * Три способа поставить тысячу под ружьё (К3).
 *
 * Разница не в числе, а в том, чем платишь: ополчением — памятью мест, набором
 * — серебром, дружиной — серебром каждый день и навсегда.
 */
export const RAISE_WAYS = ['levy', 'hire', 'retinue'] as const
export type RaiseWay = (typeof RAISE_WAYS)[number]

export const RAISE_DEFS: Record<
  RaiseWay,
  {
    readonly label: string
    readonly about: string
    /** Сколько стоит человек разом. */
    readonly upfront: number
    /** И сколько он ест в сутки. */
    readonly wage: number
    /** Как это отзывается в памяти места за каждого. */
    readonly mood: number
  }
> = {
  levy: {
    label: 'ополчение',
    about: 'От сохи и по обычаю: людей дают даром, но помнят это каждый год.',
    upfront: 0,
    wage: 0.4,
    mood: -0.35,
  },
  hire: {
    label: 'набор',
    about: 'За деньги и по своей воле: берут тех, кому больше нечем жить.',
    upfront: 12,
    wage: 1,
    mood: -0.05,
  },
  retinue: {
    label: 'дружина',
    about: 'Люди при оружии и на жалованье: дорого всегда, зато это войско, а не толпа.',
    upfront: 30,
    wage: 2.2,
    mood: 0,
  },
}

/** Что бывает, когда казна пуста (К6). */
export const EMPTY_PURSE = {
  /** Какая доля гарнизона расходится за сутки без жалованья. */
  garrisonMelt: 0.04,
  /** Насколько падает память места, где не платят гарнизону. */
  placeMood: -0.3,
  /** Через сколько суток без жалованья уходят люди двора. */
  courtLeaves: 20,
} as const

/** Сколько стоит стройка державы сверх цены самой постройки: обоз и надзор. */
export const WORKS_OVERHEAD = 1.2
