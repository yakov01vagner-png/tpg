/**
 * Ошибка и учение (этап 198) — содержимое.
 *
 * Корона ошибается с 0.8: нрав кривит её догадку, вести её поправляют. Чего не
 * было — учения. Ошибка ничему не учила: проигравший считал так же, как до
 * войны, а приём игрока проходил в десятый раз так же легко, как в первый.
 * Здесь лежит то, чем учение меряется.
 */

export const LEARN = {
  /** Насколько каждый повтор одного приёма помогает его раскусить. */
  perRepeat: 0.12,
  /** После скольких неудач корона перестаёт ходить той же дорогой. */
  blindsAfter: 2,
  /** Во сколько раз битый видит вернее того, кого не били. */
  humbles: 0.6,
  /** Сколько суток помнят приём, прежде чем он снова проходит как новый. */
  forgetsDays: 1095,
  /** Сколько строк учения показывать. */
  shows: 3,
} as const

export const LEARN_WORDS = {
  own: 'Ошибка у каждого своя: гордый преуменьшает чужое, пугливый преувеличивает.',
  cost: 'Битого учит не рассказ, а счёт: уступивший считает противника вернее.',
  onYou: 'Он учится на тебе: что прошло дважды, на третий раз ждут.',
  anew: 'Не сработавшее дважды не пробуют в третий раз — ищут другую дорогу.',
  seen: 'Видно, чему он научился и с какого дня.',
  fresh: 'Забывают со временем: через три года приём снова новый.',
} as const
