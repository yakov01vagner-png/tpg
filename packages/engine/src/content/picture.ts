/**
 * Своя картина мира (этап 118) — содержимое.
 *
 * С этапа 89 корона ошибалась в чужой силе: ошибка выводилась из расстояния и
 * нрава. Это было правдоподобно, но односторонне — на её ошибку нельзя было
 * повлиять. Здесь её картина начинает складываться из тех же вестей, что и
 * твоя: значит, твой обман попадает в её решения, а не только в её слова.
 */

export const PICTURE = {
  /** Насколько корона верит своим вестям против собственной догадки. */
  trustsWords: 0.75,
  /** Сколько суток весть о чужой силе для неё свежа. */
  freshDays: 120,
  /** Насколько шире её догадка о том, кто далеко. */
  farWider: 0.3,
  /** Раз во сколько суток она пересматривает картину. */
  reviewBeat: 30,
  /** Сколько стоит узнать, что у неё в голове. */
  askCost: 380,
} as const

export const PICTURE_WORDS = {
  own: 'У каждой короны своя картина мира, и решает она по ней, а не по правде.',
  fromWords: 'Она знает это от людей: то, что ей принесли, весит больше её догадок.',
  guess: 'Про дальних она только догадывается.',
  stale: 'Её весть стара — и решение она принимает по старой.',
  asked: 'Выведано, из чего она исходит.',
  wrong: 'Она ошибается, и ошибка эта в твою пользу.',
  costly: 'Она ошибается не в твою пользу — и готовится не к тому, что будет.',
} as const
