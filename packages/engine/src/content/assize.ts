/**
 * Суд (этап 182) — содержимое.
 *
 * Суд с 0.6 — дело раз в месяц и два-три ответа к нему. Дело приходит по
 * расписанию, доводов у сторон нет, свидетелей нет, правды, которой ты не
 * знаешь, тоже нет, а чужие суды — церковный, городской, вассальный — не
 * спорят с твоим ни о чём. Здесь лежит то, из чего суд делается судом.
 */

export const ASSIZE = {
  /** Сколько дел в месяц даёт спокойная земля. */
  quiet: 1,
  /** Насколько разбой прибавляет дел. */
  perBanditry: 4,
  /** И голод. */
  perHunger: 2,
  /** И вражда вассалов между собой. */
  perFeud: 1,
  /** Насколько взятка сдвигает решение в свою сторону. */
  bribeWeight: 0.35,
  /** С какой доли дел, решённых за серебро, о тебе начинают говорить. */
  soldAt: 0.3,
  /** Насколько родство весит в суде. */
  kinWeight: 0.25,
  /** И страх перед сильным. */
  fearWeight: 0.3,
  /** Какую долю дел забирает себе церковный суд там, где есть обитель. */
  churchTakes: 0.3,
  /** И городской там, где есть грамота. */
  townTakes: 0.4,
  /** И вассальный там, где суд оставлен вассалу. */
  lordTakes: 0.5,
} as const

export const COURT_PROOFS = ['charter', 'witness', 'custom', 'oath', 'nothing'] as const
export type ProofId = (typeof COURT_PROOFS)[number]

export const COURT_PROOF_DEFS: Record<
  ProofId,
  { readonly label: string; readonly weight: number; readonly about: string }
> = {
  charter: { label: 'грамота', weight: 3, about: 'Бумага с печатью — самый тяжёлый довод.' },
  witness: { label: 'свидетели', weight: 2, about: 'Трое соседей говорят одно и то же.' },
  custom: {
    label: 'обычай',
    weight: 2,
    about: '«Так всегда было» — и попробуй скажи, что не было.',
  },
  oath: { label: 'клятва', weight: 1, about: 'Готов поклясться на святом. Иногда этого хватает.' },
  nothing: { label: 'ничего', weight: 0, about: 'Кроме обиды, за ним нет ничего.' },
}

export const ASSIZE_WORDS = {
  unknown: 'Правды ты не знаешь: у обоих свои доводы, и оба говорят складно.',
  sold: 'О твоём суде говорят, что он продаётся. Такое не забывают.',
  fair: 'О твоём суде говорят, что он прям.',
  other: 'Здесь судят не только твоим судом.',
  waiting: 'Дела копятся: земля ждёт суда.',
} as const
