/**
 * Что на что влияет (этап 122) — содержимое.
 *
 * Семь версий у героя росли шесть атрибутов и семнадцать навыков, и половина из
 * них не делала ничего, кроме скорости собственного роста. Здесь у каждой строки
 * листа появляется дело, названное словами и числом: не «+к ловкости», а «на
 * сколько». Коэффициенты лежат здесь — логика их только читает.
 */

/** Насколько каждое очко навыка сверх десяти меняет своё дело. */
export const SHEET = {
  /** Тяжёлый труд: подкоп и осадные работы идут быстрее. */
  labourDigs: 0.012,
  /** Стойкость: дорога и голод отнимают меньше. */
  fortitudeHolds: 0.008,
  /** Стойкость: и внезапность бьёт слабее. */
  fortitudeSteady: 0.006,
  /** Концентрация: вести держатся в голове дольше. */
  focusRemembers: 6,
  /** Ловкость рук: соглядатая ловят реже. */
  sleightHides: 0.006,
  /** Ловкость рук: и доказательство добывается дешевле. */
  sleightSteals: 0.005,
  /** Атлетика: объезд державы идёт быстрее. */
  athleticsRides: 0.006,
  /** Атлетика: и разведка боем стоит меньше людей. */
  athleticsSpares: 0.004,
  /** Верховая езда: гонцы едут быстрее. */
  ridingSpeeds: 0.008,
  /** Верховая езда: и дозор видит дальше — по переходу за двадцать очков. */
  ridingSees: 0.05,
} as const

/** У какого навыка какие дела: для проверки «мёртвых нет» (А6). */
export const SKILL_PLACES: Record<string, readonly string[]> = {
  heavyWeapons: ['бой', 'дуэль', 'требования снаряжения', 'приступ'],
  hardLabour: ['подкоп под стены', 'осадные работы', 'стройка по приказу', 'переноска'],
  lightWeapons: ['бой', 'дуэль', 'снаряжение'],
  archery: ['бой', 'снаряжение', 'охота'],
  sleight: ['соглядатаи', 'кража доказательства', 'ремесло', 'воровство'],
  athletics: ['объезд державы', 'разведка боем', 'переноска', 'погоня'],
  riding: ['гонцы и связь', 'дальность дозора', 'снаряжение', 'должность маршала'],
  survival: ['походы', 'дозор в поле', 'глушь', 'снабжение'],
  magic: ['чары', 'артефакты', 'магические цепочки'],
  scholarship: ['отчёты наместников', 'ревизия', 'книги', 'сличение вестей'],
  healing: ['раны', 'мор', 'лекарское дело'],
  engineering: ['осадные машины', 'свои стены', 'мосты и стройка'],
  concentration: ['память о вестях', 'чары', 'сложная работа'],
  fortitude: ['дорога и голод', 'внезапность в бою', 'осада на измор'],
  trade: ['цены', 'кварталы', 'предприятия', 'ярмарки', 'казначей'],
  persuasion: ['разговоры', 'посольства', 'стол мира', 'чужой посол'],
  command: ['сбор войска', 'дисциплина части', 'скорость приказа', 'маршал'],
}

/** У какого атрибута какое прямое дело — не через навыки (А3 и А6). */
export const ATTRIBUTE_PLACES: Record<string, readonly string[]> = {
  strength: ['требования снаряжения', 'переноска', 'дуэль'],
  agility: ['снаряжение', 'дуэль', 'уклонение'],
  endurance: ['переноска', 'раны', 'усталость'],
  mind: ['вилка знания', 'раскрыть обман', 'скорость роста'],
  will: ['усталость от приёмов', 'внимание государя', 'скорость роста'],
  charisma: ['внимание государя', 'чужая рука в делах', 'скорость роста'],
}

export const SHEET_WORDS = {
  named: 'У каждой строки листа есть своё дело, названное числом.',
  dead: 'Мёртвых строк нет: у каждого навыка не меньше двух дел.',
  direct: 'Атрибуты работают прямо, а не только через скорость роста.',
  here: 'Лист показывает не уровень, а то, что уровень даёт здесь и сейчас.',
} as const
