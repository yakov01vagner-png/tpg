/**
 * Поход и снабжение (этап 173) — содержимое.
 *
 * Поход считался временем: столько-то часов дороги. Всё, что делает поход
 * походом, — хлеб, фураж, обоз, отставшие, — либо не считалось вовсе, либо
 * считалось врозь и после. Здесь лежит цена дороги.
 */

export const MARCH = {
  /** Сколько хлеба съедает человек в сутки похода. */
  breadPerMan: 1,
  /** И сколько фуража нужно коню. */
  fodderPerHorse: 3,
  /** Во сколько раз дороже зимний поход. */
  winter: 1.6,
  /** И насколько дешевле летний по своей земле. */
  ownLand: 0.75,
  /** Во сколько раз дороже чужая земля: подвоза нет, берут что найдут. */
  foreign: 1.35,
  /** И разорённая, где не осталось ничего. */
  ravaged: 1.8,
  /** С какого разбоя округа считается разорённой. */
  ravagedAt: 0.3,
  /** Сколько человек из сотни отстаёт за сутки перехода. */
  stragglersPer100: 0.6,
  /** И сколько заболевает, если идут голодными. */
  sickPer100: 1.2,
  /** Во сколько раз больше отставших у того, кто идёт голодным. */
  hungryStraggles: 2.2,
  /** Какая доля отставших возвращается в строй потом. */
  comesBack: 0.55,
  /** Сколько вёрст (часов пути) войско проходит за сутки. */
  hoursPerDay: 8,
  /** Сколько своего веса обоз тащит за один переход. */
  trainCarries: 40,
} as const

export const MARCH_WORDS = {
  fed: 'Обоз держит войско: хлеб есть, кони сыты.',
  thin: 'Обоз не поспевает: едят через день.',
  none: 'Обоза нет: войско кормится тем, что найдёт, и это видно по округе.',
  foreign: 'Чужая земля не кормит добром: берут силой и оставляют за собой пустоту.',
  ravaged: 'Здесь уже прошли: в округе не осталось ни зерна, ни скотины.',
  winter: 'Зимний поход стоит вдвое: ни травы коням, ни хлеба людям.',
  back: 'Отставшие догоняют неделями: половина вернётся, половина осядет по дворам.',
} as const
