/**
 * Наследники чужих путей (этап 144) — содержимое.
 *
 * Путь у короны есть (этап 140), но государи не вечны. Со сменой государя путь
 * либо переходит по наследству, либо переменяется — по нраву нового; а смута у
 * чужой короны отбрасывает её назад так же, как отбросила бы война.
 */

export const HEIRS = {
  /** Раз во сколько суток мир смотрит, не сменился ли где государь. */
  beat: 30,
  /** Ниже какой средней верности своих у короны считается смута. */
  troubledAt: 40,
  /** Насколько смута замедляет путь. */
  troubledSpeed: 0.5,
  /** Насколько смерть ведущего сбивает страх перед его державой. */
  deathCalms: 0.5,
} as const

export const HEIRS_WORDS = {
  kept: 'Путь пережил своего государя: новый ведёт то же, чем шёл отец.',
  changed: 'Новый государь — новый путь: тем, чем шёл отец, он не пойдёт.',
  died: 'Умер тот, кто вёл гонку: мир пересчитывает равновесие.',
  troubled: 'Смута у чужой короны — тот же откат, что война: путь стоит, пока свои спорят.',
  yours: 'Ты умер — и мир считает заново, но уже относительно твоего наследника.',
} as const
