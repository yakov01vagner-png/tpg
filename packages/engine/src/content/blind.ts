/**
 * Осада вслепую (этап 113) — содержимое.
 *
 * Осада 0.7 считалась при открытых картах: осаждающий видел чужой запас числом,
 * осаждённый знал, идёт ли к нему выручка. Обе стороны решали задачу на счёт.
 * Здесь у стен появляется то, ради чего под ними вообще стоят: обе стороны
 * гадают, и обе могут этим воспользоваться.
 */

/** Кто приходит из-за стен и зачем. */
export const DEFECTOR_KINDS = ['hungry', 'bought', 'planted', 'faithful'] as const
export type DefectorKind = (typeof DEFECTOR_KINDS)[number]

export const DEFECTOR_DEFS: Record<
  DefectorKind,
  {
    readonly label: string
    readonly about: string
    /** Правду ли говорит. */
    readonly true: boolean
    /** Насколько расходится его число с правдой, долей. */
    readonly off: number
  }
> = {
  hungry: {
    label: 'голодный',
    about: 'Перелез ночью, потому что в городе нечего есть. Врать ему незачем.',
    true: true,
    off: 0.12,
  },
  bought: {
    label: 'купленный',
    about: 'Пришёл за серебром. Скажет то, за что платят, — и немного больше.',
    true: true,
    off: 0.3,
  },
  planted: {
    label: 'подосланный',
    about: 'Его выпустили нарочно. Он скажет, что хлеба вдоволь, и будет очень убедителен.',
    true: false,
    off: 0.8,
  },
  faithful: {
    label: 'верный своим',
    about: 'Пришёл выведать твоё, а не рассказать своё. Уйдёт обратно той же ночью.',
    true: false,
    off: 0.5,
  },
}

export const BLIND = {
  /** С какой вилки начинает тот, кто не знает о городе ничего. */
  blindSpread: 0.55,
  /** Насколько сужается догадка о запасе за каждые десять суток осады. */
  narrowsPerTenDays: 0.12,
  /** Насколько вылазка открывает силы гарнизона. */
  sallyShows: 0.35,
  /** Сколько суток идёт весть о выручке к осаждённым. */
  reliefWordDays: 4,
  /** Насколько блеф вообще может сдвинуть условия. */
  bluffWorth: 0.35,
  /** Раз во сколько суток из-за стен кто-нибудь приходит. */
  defectorBeat: 6,
  /** Сколько стоит купить перебежчика. */
  buyDefector: 180,
  /** На сколько переходов видны знамёна ложной выручки. */
  bannerHops: 3,
} as const

export const BLIND_WORDS = {
  guessing: 'Сколько у них хлеба — никто снаружи не знает. Считают по дыму и по вылазкам.',
  narrow: 'Стоим долго: видно, чем они топят и как часто выходят.',
  noRelief: 'Идёт ли к ним помощь — за стенами не знают тоже.',
  bluffed: 'Ты говорил уверенно, и тебе поверили.',
  called: 'Блеф раскрыли: теперь твоим словам под этими стенами не верят.',
  came: 'Ночью из города перелез человек.',
  lied: 'Он говорил складно. Слишком складно.',
  banners: 'На холме встали знамёна. Чьи — отсюда не разобрать.',
  lifted: 'Осаду сняли: побоялись оказаться между войском и стенами.',
} as const
