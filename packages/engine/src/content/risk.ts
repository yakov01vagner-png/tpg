/**
 * Цена риска и старые долги (этап 153) — содержимое.
 *
 * Между «всё хорошо» и «конец» была пустота: проигранная война отнимала землю и
 * на этом кончалась. Здесь у поражения появляются степени, у каждой имя, и то,
 * чем платят на каждой.
 */

/** Шесть степеней поражения (Рс2). */
export const DEFEATS = ['scratch', 'toll', 'tribute', 'loss', 'vassal', 'fall'] as const
export type DefeatId = (typeof DEFEATS)[number]

export const DEFEAT_DEFS: Record<
  DefeatId,
  { readonly label: string; readonly says: string; readonly costs: string }
> = {
  scratch: {
    label: 'царапина',
    says: 'Отбились: людей потеряли, земли не отдали.',
    costs: 'людьми и годом',
  },
  toll: { label: 'откуп', says: 'Откупились серебром: война кончилась дорого.', costs: 'казной' },
  tribute: {
    label: 'дань',
    says: 'Платишь дань: каждый день напоминает, кто кого разбил.',
    costs: 'доходом на годы',
  },
  loss: {
    label: 'потеря земли',
    says: 'Земля ушла: то, что взято, назад просят войной.',
    costs: 'местами',
  },
  vassal: {
    label: 'под руку',
    says: 'Держава цела, но она уже не своя: ты под чужой рукой.',
    costs: 'свободой',
  },
  fall: { label: 'падение', says: 'Державы нет: осталось то, чего не отнять.', costs: 'всем' },
}

/** Чем оборонять своё (Рс0). */
export const HOLDS = ['hold', 'sally', 'pay'] as const
export type HoldId = (typeof HOLDS)[number]

export const HOLD_DEFS: Record<HoldId, { readonly label: string; readonly says: string }> = {
  hold: { label: 'держаться', says: 'Сидеть за стенами: запас тает, но стены стоят.' },
  sally: { label: 'вылазка', says: 'Выйти и ударить: можно снять осаду, можно потерять всё.' },
  pay: { label: 'откупиться', says: 'Заплатить осаждающим: дёшево для людей, дорого для казны.' },
}

export const RISK = {
  /** Во сколько обходится откуп от осады — за каждого осаждающего. */
  payPerMan: 26,
  /** Насколько своё войско сильнее за стенами. */
  wallsWorth: 1.35,
  /** Сколько людей теряет неудачная вылазка — доля отряда. */
  sallyLoss: 0.3,
  /** Насколько падает верность двора после проигранной войны. */
  courtAfter: -12,
  /** Насколько холодеет мир к добиваемому. */
  finishing: -15,
} as const

export const RISK_WORDS = {
  named: 'Риск назван заранее: видно, чем именно можно поплатиться.',
  degrees: 'Между «всё хорошо» и «конец» шесть степеней, и у каждой имя.',
  debts: 'Проигранная война оставляет долги, дань и гарантии — и это играется дальше.',
  court: 'Двор после поражения ведёт себя иначе, и это видно числами.',
  theirs: 'Проигравшую корону можно добить, а можно поднять: и то и другое считается.',
  walls: 'Свою крепость можно оборонять, а не только брать чужую.',
  other: 'В чужой бой можно прийти — на любой стороне.',
} as const
