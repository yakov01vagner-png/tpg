/**
 * Город и вольности (этап 184) — содержимое.
 *
 * Город с 0.7 — место с казной, настроением черни и купцов и вольностью, у
 * которой два состояния: есть или нет. Внутри него никого нет: ни цехов, ни
 * магистрата, ни ополчения, — а вольность не обсуждается, она выдаётся. Здесь
 * лежит то, из чего город делается городом.
 */

export const GUILDS = ['smiths', 'weavers', 'tanners', 'merchants', 'masons', 'boatmen'] as const
export type GuildId = (typeof GUILDS)[number]

export const GUILD_DEFS: Record<
  GuildId,
  {
    readonly label: string
    readonly about: string
    readonly wants: string
    readonly needs?: string
  }
> = {
  smiths: {
    label: 'кузнечный цех',
    about: 'Держит кузни и цену на железо. Спорить с ними — остаться без подков.',
    wants: 'заказ на оружие и право не пускать чужих кузнецов',
    needs: 'smithy',
  },
  weavers: {
    label: 'суконный цех',
    about: 'Ткёт, красит и считает лучше многих.',
    wants: 'пошлину на привозное сукно',
  },
  tanners: {
    label: 'кожевенный цех',
    about: 'Работа вонючая, деньги хорошие, место у реки.',
    wants: 'своё место за стеной и воду',
  },
  merchants: {
    label: 'купеческая сотня',
    about: 'Те, у кого деньги. Разговор с городом — это разговор с ними.',
    wants: 'охрану дорог и своё слово в магистрате',
    needs: 'market',
  },
  masons: {
    label: 'артель каменщиков',
    about: 'Строит стены и башни — тем и держит город за горло.',
    wants: 'подряд на стены',
  },
  boatmen: {
    label: 'артель лодочников',
    about: 'Возит всё, что плывёт, и знает всех на реке.',
    wants: 'право перевоза и низкую пошлину',
  },
}

export const BURGH = {
  /** Сколько ополчения даёт тысяча горожан. */
  militiaPer1000: 22,
  /** Во сколько раз охотнее город даёт людей, когда доволен. */
  gladGives: 1.4,
  /** Какую долю казны город даёт взаймы. */
  lends: 0.35,
  /** Под какой процент. */
  rate: 0.08,
  /** Во сколько обходится вольность: доля годовой подати города. */
  charterCosts: 1.6,
  /** Сколько цехов бывает в городе. */
  guildsMost: 4,
  /** С какого расстояния города считаются соперниками. */
  rivalHops: 8,
  /** Ниже какого настроения черни начинается бунт. */
  riotsBelow: -45,
} as const

export const BURGH_WORDS = {
  guilds: 'Город — это цехи: с ними и договариваются.',
  charter: 'Вольность — договор, а не флаг: в ней написано, что город отдаёт и что берёт.',
  lends: 'Город даёт взаймы охотнее, чем платит подать: заём он считает вложением.',
  rivals: 'Соседний город берёт тот же товар и ту же дорогу — и не рад этому.',
  militia: 'Городское ополчение — не дружина: оно защищает свои стены и дальше не идёт.',
  riot: 'Чернь на площади. Купцы заперли лавки.',
} as const
